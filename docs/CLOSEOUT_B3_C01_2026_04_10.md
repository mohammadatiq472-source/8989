# 收尾总览（B3-C01，2026-04-10）

## 0. 结论

`B3-C01` 已完成：`gate:ai:trio` 现已启用 nightly “复用最新报告”模式，避免重复跑 session/mainline，且增加报告新鲜度硬校验。

## 1. 代码改动

1. `server/src/evals/runAiNightlyAcceptanceGate.ts`
   - 新增 `NIGHTLY_REUSE_LATEST_GATES` 模式
   - 新增 `NIGHTLY_REUSE_MAX_REPORT_AGE_SEC`（默认 900）
   - 新增检查：
     - `mainline_report_fresh_when_reuse`
     - `template_replay_report_fresh_when_reuse`
     - `session_security_report_fresh_when_reuse`
   - 报告新增：
     - `executionMode`
     - `reuseMaxReportAgeSec`
2. `package.json`
   - `gate:ai:trio` 改为：
     - 先跑 `session + mainline`
     - nightly 使用 `NIGHTLY_REUSE_LATEST_GATES=1`

## 2. 验证链

1. `npm run build` -> `PASS`
2. `npm run gate:ai:trio` -> `PASS`
3. `tmp/gates/ai-nightly-acceptance/ai_nightly_acceptance_latest.json` 抽样：
   - `executionMode = reuse_latest_reports`
   - `template_replay_*` 检查全 `true`
   - `*_report_fresh_when_reuse` 检查全 `true`

## 3. 影响

1. 三件套门禁执行时间显著下降（不再在 nightly 内重复二次执行 session/mainline）。
2. 仍保持安全性：复用模式要求报告“足够新鲜”，否则 nightly 直接 fail。
