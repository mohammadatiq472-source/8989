# M02 - World Rule Kernel

## Definition
- Category: Backend
- Owner AI engineer: AI-BE-RuleState
- Backup AI engineer: AI-Agent-Commander
- Goal: Authoritative world state, tick execution, plan queue, map diff and mutation guarantees.

## Code Boundaries
- `server/src/application/world/`
- `server/src/routes/world.ts`

## Official Entrypoints
- POST /api/world/action
- GET /api/world

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run test:world:mutation-lock` -> `npm run eval:orchestrator:stress`
- Lint (global): `PASS`
- Build: `PASS` (`vite build` completed; bundle-size warnings only)
- World lock test: `PASS` (`[world_mutation_lock] all checks passed`)
- Orchestrator stress: `PASS` (`successRate=1`, `failureCount=0`, `totalAgents=24`)
- Module change set: extracted persistence and narrative storage from `WorldService.ts` to `server/src/application/world/persistence/worldPersistence.ts` while preserving `WorldService` facade behavior
- Snapshot updated at: `2026-03-26 22:09 CST`
