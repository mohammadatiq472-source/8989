# M12 - MCP Integration

## Definition
- Category: Backend/Platform
- Owner AI engineer: AI-Platform-ModelGateway
- Backup AI engineer: AI-Platform-Observability
- Goal: MCP tool bridge for coding assistants with safe output shaping and backend proxy calls.

## Code Boundaries
- `server/src/mcp/`

## Official Entrypoints
- npx tsx server/src/mcp/gameServer.ts

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C02)
- Scope: MCP `get_ai_logs` 文案同步 strict/fallback 可观测字段语义。
- Changed:
  - `server/src/mcp/gameServer.ts`
- Validation chain:
  - `npm run build` (PASS)
  - MCP smoke（通过 `get_ai_logs` 对应后端 `/api/ai/logs` 返回结构核验）(PASS)
- Expected:
  - MCP 侧调用者可明确知道日志里存在 strict/fallback 诊断字段，不再误解为仅文本日志。

## Validation Update (2026-04-09, W2-C13)
- Scope: MCP tool 注释/文案编码清扫（仅文案层，不改 tool API 行为）。
- Changed:
  - `server/src/mcp/gameServer.ts`
- Changes detail:
  - 统一 tool 区块注释风格为稳定 ASCII/Bilingual 可读文案。
  - 精简并标准化 tool description 文案，避免历史混排造成的解析歧义。
  - 保持 tool 名称、参数 schema、后端调用路径、返回结构不变。
- Validation chain:
  - `npm run build` -> `PASS`
  - MCP smoke（`health_check` + `get_world_summary` via stdio client）-> `PASS`

## Validation Update (2026-04-10, B2-C17)
- Scope: MCP 从“读链为主”扩展到最小可执行控制面（session + world action）。
- Changed:
  - `server/src/mcp/gameServer.ts`
- New tools:
  - `get_session_runtime`
  - `join_session`
  - `set_session_autonomy`
  - `leave_session`
  - `get_map_layout`
  - `run_world_action`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run godot:ops:cli -- runtime` -> `PASS`
  - `npm run godot:ops:cli -- world-action --action advanceTick` -> `PASS`
- Expected:
  - AI 编程助手可通过 MCP 直接完成 session 进入/自治切换/离开与 world action 执行，不再只停留在读取态。

## Validation Update (2026-04-10, B2-C19)
- Scope: MCP 操作面从“通用 world action”扩展到“模板化执行层”（含 move/upgrade/override 自动 payload 解析）。
- Changed:
  - `server/src/mcp/gameServer.ts`
- New tools:
  - `list_world_action_templates`
  - `run_world_action_template`
- Key outcomes:
  - 增加模板目录输出，后续 AI 可先读模板再执行，减少手工拼 payload。
  - 模板执行支持 `advance_tick / clear_plan_execution / preview_* / move_first_unit / upgrade_first_city / tactical_override_first_unit`。
  - 对于 move/override 模板，自动补齐 faction/unit/target 解析（含 map-layout fallback）。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run godot:ops:cli -- world-action-templates` -> `PASS`
  - `npm run godot:ops:cli -- world-action-template --template move_first_unit --include-world` -> `PASS`

## Validation Update (2026-04-10, B2-C20)
- Scope: 将模板动作链路从“可执行”固化为“可回归”（剧本执行 + events/narratives 对账报告）。
- Changed:
  - `godot-client/tools/slg_ops_cli.py`
  - `server/src/evals/runAiMainlineStabilityGate.ts`
- Key outcomes:
  - CLI 新增 `template-replay` 子命令，内置 `baseline_v1` 剧本，执行模板动作后自动对账 `/api/events` 与 `/api/narratives`。
  - 主线 gate 增加模板回放校验并自动产出 `tmp/gates/ai_ops_template_replay_latest.json`。
  - `move_first_unit` 在 B2-C20 阶段曾按“可选步骤失败”处理；该策略已在 B2-C21 收紧为必选步骤。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:mainline:stability` -> `PASS`
  - `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 world-action-template --template advance_tick` -> `PASS`
  - `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 template-replay --scenario baseline_v1 --report-path tmp/gates/ai_ops_template_replay_cli_latest.json` -> `PASS`

## Validation Update (2026-04-10, B2-C21)
- Scope: 模板回放收口为固定初始态夹具 + 全必选步骤，减少运行态漂移噪音。
- Changed:
  - `godot-client/tools/slg_ops_cli.py`
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiMainlineStabilityGate.ts`
- Key outcomes:
  - 新增 `POST /api/save-slots/fixture/prime`，支持 `initial_world_v1/current_world` 作为回放夹具源。
  - `template-replay` 新增 `--fixture-slot-id/--backup-slot-id/--fixture-source/--restore-world`。
  - `move_first_unit` 等剧本步骤全部 `required=true`，不再保留可选成功项。
  - 回放链路内部超时下限提升到 `60s`，避免 `advance_tick` 长耗时被误判。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:mainline:stability` -> `PASS`
  - `py -3.11 godot-client/tools/slg_ops_cli.py --base-url http://127.0.0.1:8799 template-replay --scenario baseline_v1` -> `PASS`
