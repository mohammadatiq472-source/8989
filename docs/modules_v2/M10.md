# M10 - AI Runtime Config and Copilot

## Definition
- Category: Backend/Platform
- Owner AI engineer: AI-Platform-ModelGateway
- Backup AI engineer: AI-Agent-Commander
- Goal: Model catalog/config, copilot runtime bridge, and AI server proxy integration.

## Code Boundaries
- `server/src/application/ai/`
- `server/src/routes/ai.ts`
- `server/src/routes/copilot.ts`
- `server/src/ai-server.ts`

## Official Entrypoints
- GET|POST /api/ai/config
- GET /api/ai/models
- POST /api/copilotkit

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `FAIL` (`1` errors, `0` warnings)
- Module lint hit files:
  - `server/src/ai-server.ts` (E1/W0)
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C02)
- Scope: 网关 strict/fallback 可观测字段接入 `GET /api/ai/logs`。
- Changed:
  - `server/src/application/planning/PlanningService.ts`
  - `server/src/routes/ai.ts`
- Validation chain:
  - `npm run build` (PASS)
  - `npm run sim:quick` (执行完成，退出码 `1`，原因为评测脚本检测到既有 critical gap，非本次改动引入)
  - `curl /api/ai/logs?limit=20` (PASS，返回 `gatewayStrictMode`、`fallbackUsedCount`，单条日志含 `strictMode/fallbackUsed/fallbackCause`)
- Expected:
  - 网关模式日志可直接区分 strict on/off。
  - fallback 原因可从日志字段读取，不再仅依赖 message 文本。

## Validation Update (2026-04-09, B2-C05)
- Scope: `AiConfigService` 配置层最小持久化（按 faction 保存 AI Hub config + updatedAt）。
- Changed:
  - `server/src/application/ai/AiConfigService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `AiConfigService` 从内存 Map 改为带磁盘持久化（默认 `tmp/ai_hub_configs.json`，支持 `AI_HUB_CONFIG_STORE_PATH` 覆盖）。
  - 落盘前使用 `aiHubConfigSchema` 校验，加载异常配置会跳过，避免污染运行态。
  - 写盘采用 debounce + 原子写，并在进程关停时接入统一 flush。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8797 npm run start` + `POST /api/ai/config` + 重启后 `GET /api/ai/config?factionId=wei` -> `PASS`

## Validation Update (2026-04-09, B2-C06)
- Scope: AI Hub 配置持久化鲁棒性补强（版本封装 + 损坏文件隔离恢复）。
- Changed:
  - `server/src/application/ai/AiConfigService.ts`
- Behavior updates:
  - 持久化文件增加 envelope 元信息：`version/savedAt`。
  - 继续兼容旧格式（仅 `configs/updatedAt`）读取。
  - 若 JSON 损坏，自动隔离到 `*.corrupt.<ts>` 并清空内存态，服务可继续运行。
- Validation chain:
  - `npm run build` -> `PASS`
  - legacy object 文件加载 smoke -> `PASS`
  - corrupt JSON 文件隔离 smoke -> `PASS`

## Validation Update (2026-04-09, B2-C07)
- Scope: 与 Model Gateway 相关的 BYOK 安全约束回写（密钥托管要求）。
- Changed:
  - `server/src/application/faction/FactionConfigStore.ts`（BYOK 持久化安全策略）
- Notes:
  - `apiKey` 持久化现在受 `FACTION_APIKEY_ENCRYPTION_KEY` 控制，建议在部署层统一注入并轮换。
  - 未配置密钥时，系统默认不落盘 `apiKey`（除非显式启用 `FACTION_APIKEY_ALLOW_PLAINTEXT_PERSIST=1`）。
- Validation chain:
  - `npm run build` -> `PASS`
  - API 配置写入 + 文件检查 + 重启解密 smoke -> `PASS`

## Validation Update (2026-04-09, B2-C08)
- Scope: AI 配置持久化健康指标输出（模型配置层可观测）。
- Changed:
  - `server/src/application/ai/AiConfigService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - 新增 `getAiHubConfigPersistHealth()`，在 `/api/health.persistence.aiConfig` 输出：
    - `factionCount`
    - `persistSuccessCount/persistFailureCount`
    - `corruptQuarantineCount`
- Validation chain:
  - `npm run build` -> `PASS`
  - `GET /api/health` -> `PASS`（存在 `persistence.aiConfig`）

## Validation Update (2026-04-09, B2-C09)
- Scope: AI 配置持久化失败/隔离事件纳入启动告警分级。
- Changed:
  - `server/src/app.ts`
- Behavior updates:
  - `persistence.aiConfig.persistFailureCount > 0` 时输出 `persist_failures`（阈值 `>=5` 升级 HIGH）。
  - `persistence.aiConfig.corruptQuarantineCount > 0` 时输出 `quarantine_detected/surge`（阈值 `>=3` 升级 HIGH）。
  - 以上告警同时进入 `/api/health.persistence.alerts` 与启动日志，便于模型配置层值班排障。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8788 npm run start` + `GET /api/health` -> `PASS`（alerts 字段可读）
