# M15 - Frontend Command Workspace

## Definition
- Category: Frontend
- Owner AI engineer: AI-FE-CommandSurface
- Backup AI engineer: AI-FE-MapSurface
- Goal: Main UI shell, command panels, data sync, and interaction workflow orchestration.

## Code Boundaries
- `src/main.tsx`
- `src/App.tsx`
- `src/components/layout/`
- `src/components/panels/`
- `src/components/screens/`
- `src/api/`
- `src/components/copilot/`

## Official Entrypoints
- npm run dev

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build`
- Lint (global): `PASS`
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Scope change: extracted tile action preview / path estimation flow from `src/App.tsx` into `src/app/flows/tileActionPreview.ts`
- Snapshot updated at: `2026-03-26 21:17 CST`

## Validation Update (2026-04-09, W2-C11)
- Scope: Godot command surface 将 runtime/session/autonomy 展示标准化（只消费后端状态，不做前端权威分支）。
- Changed files:
  - `godot-client/autoload/session_store.gd`
  - `godot-client/scripts/app/main.gd`
  - `godot-client/scripts/ui/observability_panel.gd`
  - `godot-client/scenes/ui/observability_panel.tscn`
  - `godot-client/README.md`
- Key outcomes:
  - `SessionStore` 增加 `autonomy_level` / `control_mode` 持久字段。
  - `main.gd` 启动链路统一解析 `/api/session/runtime` 与 `/api/session/join`，生成单一 runtime context。
  - `ObservabilityPanel` 新增 `Runtime` 子区块，统一展示 `controlMode/autonomy/seat/sessionMode`。
- Validation chain:
  - `D:\\Apps\\Godot\\Godot_v4.6.2-stable_win64_console.exe --headless --path godot-client --quit-after 1` -> `PASS`
  - `npm run gate:godot:week1` -> `PASS`

## Validation Update (2026-04-10, B2-C14)
- Scope: Runtime 观测链从“启动时一次读取”扩展到持续只读轮询（`/api/session/runtime`），并在 Godot ObservabilityPanel 输出 runtime API 对账信息。
- Changed files:
  - `godot-client/scripts/infra/observability/observability_bridge.gd`
  - `godot-client/scripts/ui/observability_panel.gd`
  - `godot-client/scenes/ui/observability_panel.tscn`
  - `godot-client/scripts/app/main.gd`
  - `godot-client/README.md`
- Key outcomes:
  - Runtime 子区块新增 `runtimeApi pollOk/fail/tick/worldVersion/factions/apiMode/apiAutonomy/apiSeats`。
  - 保留 `main.gd + SessionStore` 现有 session 上下文，不新增前端权威分支。
- Validation chain:
  - `D:\\Apps\\Godot\\Godot_v4.6.2-stable_win64_console.exe --headless --path godot-client --quit-after 1` -> `PASS`
  - `npm run gate:godot:week1` -> `PASS`
  - `PORT=8798 npm run start + GET /api/session/runtime` -> `PASS`

## Validation Update (2026-04-10, B2-C17)
- Scope: 新增 Godot/后端统一 CLI 控制面，补齐“可执行链”入口。
- Changed files:
  - `godot-client/tools/slg_ops_cli.py`
  - `godot-client/README.md`
  - `package.json`
- Key outcomes:
  - 提供 `health/runtime/join/autonomy/leave/world/map-layout/world-action/advance-tick/headless/bootstrap-chain` 一组正式子命令。
  - `bootstrap-chain` 可一次性复现 `health -> runtime -> join -> world -> map-layout -> headless -> leave` 并输出 JSON 证据。
  - npm 统一入口：`npm run godot:ops:cli -- <subcommand>`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run godot:ops:cli -- health` -> `PASS`
  - `npm run godot:ops:cli -- runtime` -> `PASS`
  - `npm run godot:ops:cli -- bootstrap-chain --output tmp/gates/godot_ops_bootstrap_latest.json` -> `PASS`

## Validation Update (2026-04-10, B2-C19)
- Scope: Godot/CLI 操作面模板化，补“可执行链 -> 常用动作模板链”。
- Changed files:
  - `godot-client/tools/slg_ops_cli.py`
  - `godot-client/scripts/app/main.gd`
  - `godot-client/scripts/ui/observability_panel.gd`
  - `godot-client/README.md`
- Key outcomes:
  - CLI 新增 `world-action-templates` 与 `world-action-template` 子命令。
  - 模板动作覆盖 `advance_tick / clear_plan_execution / preview_* / move_first_unit / upgrade_first_city / tactical_override_first_unit`。
  - Godot 增加快捷模板动作（`Ctrl+1/2/3`）与 Runtime `lastAction/status/tick` 回显。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run godot:ops:cli -- world-action-templates` -> `PASS`
  - `npm run godot:ops:cli -- world-action-template --template advance_tick --include-world` -> `PASS`
  - `npm run godot:ops:cli -- world-action-template --template move_first_unit --include-world` -> `PASS`
  - `npm run gate:godot:week1` -> `PASS`

## Validation Update (2026-04-10, B2-C20)
- Scope: 在 CLI 控制面补“模板剧本回归”正式入口，固化事件/叙事对账证据。
- Changed files:
  - `godot-client/tools/slg_ops_cli.py`
  - `godot-client/README.md`
- Key outcomes:
  - 新增 `template-replay` 子命令（`baseline_v1`），统一执行模板动作并自动输出对账报告。
  - 默认报告路径：`tmp/gates/ai_ops_template_replay_latest.json`（可通过 `--report-path` 覆盖）。
  - `move_first_unit` 在 B2-C20 阶段曾为可选成功项；该策略已在 B2-C21 收紧为必选步骤。
- Validation chain:
  - `py -3.11 -m py_compile godot-client/tools/slg_ops_cli.py` -> `PASS`
  - `npm run build` -> `PASS`
  - `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 template-replay --scenario baseline_v1 --report-path tmp/gates/ai_ops_template_replay_cli_latest.json` -> `PASS`

## Validation Update (2026-04-10, B2-C21)
- Scope: CLI 模板回放补固定初始态夹具与恢复链，降低随机运行态干扰。
- Changed files:
  - `godot-client/tools/slg_ops_cli.py`
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
- Key outcomes:
  - `template-replay` 增加 `--fixture-slot-id/--backup-slot-id/--fixture-source/--restore-world`。
  - 回放默认 `fixture-source=initial_world_v1`，自动执行 `backup -> prime fixture -> load fixture -> replay -> restore`。
  - 回放内链最小超时自动提升到 `60s`，减少 `advance_tick` 超时误报。
- Validation chain:
  - `py -3.11 -m py_compile godot-client/tools/slg_ops_cli.py` -> `PASS`
  - `npm run build` -> `PASS`
  - `py -3.11 godot-client/tools/slg_ops_cli.py --base-url http://127.0.0.1:8799 template-replay --scenario baseline_v1` -> `PASS`
