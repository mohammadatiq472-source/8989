# M11 - Observability Replay Streaming

## Definition
- Category: Backend/Platform
- Owner AI engineer: AI-Platform-Observability
- Backup AI engineer: AI-Platform-ModelGateway
- Goal: Event/narrative/replay pipelines, RAG retrieval, SSE/WS streaming and save slots.

## Code Boundaries
- `server/src/routes/observability.ts`
- `server/src/routes/replay.ts`
- `server/src/ws/`
- `server/src/infra/rag/`
- `server/src/config/replayRag.ts`

## Official Entrypoints
- GET /api/events
- GET /api/events/stream
- GET /api/replay/archive
- WS /ws

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C04)
- Scope: 扩展 `WS/Events` 可观测指标（连接数、订阅数、按势力分布、最近错误）并接入 Godot HUD。
- Changed:
  - `server/src/ws/GameWebSocket.ts`
  - `server/src/routes/observability.ts`
  - `shared/contracts/game/history.ts`
  - `godot-client/scripts/infra/observability/observability_bridge.gd`
  - `godot-client/scripts/ui/observability_panel.gd`
  - `godot-client/scripts/app/main.gd`
- Validation chain:
  - `npm run build` (PASS)
  - `npm run gate:godot:week1` (PASS)
  - `node tsx app(:8788) + ws invalid-json smoke + curl /api/events?limit=5` (PASS，返回 `wsStats` 且 `recentErrors` 可观测)
- Expected:
  - `/api/events` 响应包含 `wsStats.totalConnections/subscribedConnections/factionDistribution/recentErrors`。
  - Godot `ObservabilityPanel` 可直接显示服务端 WS 连接态与最近错误摘要，便于性能和稳定性对比。

## Validation Update (2026-04-10, B2-C14)
- Scope: Godot 只读可观测面板补齐 `/api/events` + `/api/civil-memory` + `/api/session/runtime` 三条读链，不引入前端权威分支。
- Changed:
  - `godot-client/scripts/infra/http/backend_api_client.gd`
  - `godot-client/scripts/infra/observability/observability_bridge.gd`
  - `godot-client/scripts/ui/observability_panel.gd`
  - `godot-client/scenes/ui/observability_panel.tscn`
  - `godot-client/scripts/app/main.gd`
  - `godot-client/README.md`
- Validation chain:
  - `npm run build` (PASS)
  - `D:\\Apps\\Godot\\Godot_v4.6.2-stable_win64_console.exe --headless --path godot-client --quit-after 1` (PASS)
  - `npm run gate:godot:week1` (PASS)
  - `PORT=8798 npm run start + GET /api/events?limit=3 + GET /api/civil-memory?limit=3 + GET /api/session/runtime` (PASS)
- Expected:
  - `ObservabilityBridge` 周期读取 `events/runtime/civil-memory`，并输出统一 snapshot。
  - `ObservabilityPanel` 新增 `CivilMemory` 子区块，`Runtime` 子区块追加 runtime API poll 指标。
  - 客户端仍保持只读消费后端状态，不增加任何前端 authoritative 规则分支。
