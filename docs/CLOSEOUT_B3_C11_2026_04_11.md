# 收尾总览（B3-C11，2026-04-11）

## 0. 结论

`B3-C11` 已完成：`save-slots-restore-apply-gate` 产物已收敛为 `latest + 最近 N 份` 保留策略，并且 restore-apply 关键结果已并入 trio summary。

## 1. 改动文件

1. `server/src/evals/runSaveSlotsRestoreApplyGate.ts`
2. `server/src/evals/runGateTrioSummary.ts`
3. `docs/modules_v2/M01.md`
4. `docs/modules_v2/M18.md`
5. `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
6. `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
7. `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`
8. `docs/AI_ENGINEER_HUB_2026_03_25.md`
9. `docs/CLOSEOUT_B3_C11_2026_04_11.md`

## 2. 行为变更

1. restore-apply gate 产物保留策略：
   - 固定保留 `tmp/gates/save-slots-restore-apply-gate/save_slots_restore_apply_gate_latest.json`
   - 保留最近 `N` 份 stamped 报告与同 runId 的 `_workspace`
   - 超额历史产物自动清理
2. 新增保留参数：
   - `SAVE_SLOTS_RESTORE_APPLY_GATE_KEEP_RECENT`（默认 `12`，范围 `1~200`）
3. restore-apply gate 报告新增 `artifacts` 字段：
   - `keepRecent`
   - `retainedRunIds`
   - `prunedRunIds`
   - `prunedReports`
   - `prunedWorkspaces`
4. trio summary 新增 `reports.saveSlotsRestoreApply`：
   - 汇总 restore 三段状态（`first/second/third`）
   - 汇总健康计数（`restoreApplySuccessCount/restoreApplyFailureCount`）
   - 汇总产物保留统计（`keepRecent/retainedCount/prunedCount`）
5. trio summary 新增三项检查：
   - `save_slots_restore_apply_gate_passed`
   - `nightly_references_latest_restore_apply_gate`
   - `save_slots_restore_apply_three_phase_semantics`

6. 并发清理护栏（补强）：
   - workspace 清理仅针对“已淘汰 stamped 报告 runId”，不再按“非保留即删”策略处理，避免并发 gate 误删在跑目录

## 3. 验证链

1. `npm run build` -> `PASS`
2. `npm run gate:ai:nightly:acceptance` -> `PASS`（runId: `ai_nightly_2026-04-11T07-36-42-175Z`）
3. `npm run gate:ai:trio` -> `PASS`（nightly runId: `ai_nightly_2026-04-11T07-44-05-114Z`，summary runId: `gate_trio_summary_2026-04-11T07-44-47-384Z`）
4. 抽样检查：
   - restore-apply latest 含 `artifacts.keepRecent=12`
   - trio summary latest 含 `reports.saveSlotsRestoreApply`
   - trio summary checks 三项新增检查均为 `true`

## 4. 风险与边界

1. 当前默认 `keepRecent=12` 下未触发真实裁剪样本（`pruned*` 可能为 `0`），但策略路径与统计字段已可观测。
2. 本卡已补并发清理护栏；仍建议避免在同一工作目录并发触发多条 nightly/trio 门禁链。
3. 本卡仅收敛 `save-slots-restore-apply-gate` 产物；`gate-trio` summary 目录仍按历史模式保留。

## 5. 下一步建议

1. 推进 `B3-C12`：给 `gate-trio` summary 增加 `latest + 最近 N 份` 保留策略，并补 summary 新鲜度显式检查。
