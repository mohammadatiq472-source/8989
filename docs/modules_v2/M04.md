# M04 - General Operations and Chat

## Definition
- Category: Backend/Agent
- Owner AI engineer: AI-Agent-General
- Backup AI engineer: AI-Agent-Commander
- Goal: General-level execution, tier gating, profile persistence, and dual-mode chat/order interaction.

## Code Boundaries
- `server/src/agents/general/GeneralAgent.ts`
- `server/src/agents/general/GeneralUtilityAI.ts`
- `server/src/agents/general/GeneralLLMAdapter.ts`
- `server/src/agents/general/GeneralProfileStore.ts`
- `server/src/agents/general/GeneralChatService.ts`
- `server/src/routes/generalChat.ts`

## Official Entrypoints
- GET /api/generals
- POST /api/generals/:id/chat

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C05)
- Scope: 为将领协商链 `NegotiationInbox` 增加最小持久化与重启恢复，避免进程重启后消息直接丢失。
- Changed:
  - `server/src/agents/general/GeneralNegotiationChannel.ts`
  - `server/src/app.ts`
- Validation chain:
  - `npm run build` (PASS)
  - `npm run sim:quick` (执行完成，退出码 `1`，原因为既有 critical gap 门禁，非本次改动引入)
  - `start:clock(:8790) + curl /api/generals + curl /api/events?limit=50` (PASS)
  - `tsx module smoke（注入 1 条 negotiation -> flush -> 新进程恢复）` (PASS，恢复到 `pending=1`，清理后回到 `pending=0`)
- Expected:
  - 协商 inbox 写入 `tmp/general_negotiation_inbox.json`（UTF-8 JSON）并可在下次进程启动恢复。
  - `drainNegotiationInbox` 清空后会落盘，避免“已消费消息”在重启后重复出现。
