# M13 - V2 Growth Gameplay

## Definition
- Category: Backend/Game
- Owner AI engineer: AI-BE-WorldMeta
- Backup AI engineer: AI-BE-WorldMeta
- Goal: Recruit/star/army/alliance growth systems and V2 state orchestration.

## Code Boundaries
- `server/src/application/v2/`
- `server/src/routes/v2game.ts`

## Official Entrypoints
- GET /api/v2/state
- POST /api/v2/recruit|star-upgrade|army/*

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C10)
- Scope: 收敛 V2 growth 子系统与 world authoritative 链路的状态口径。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/routes/v2game.ts`
  - `server/src/application/world/WorldService.ts`（调用侧同步钩子）
- Key outcomes:
  - `V2GameService` 增加主世界对齐能力（auto player 同步 + 资源镜像 + world<->v2 对账快照）。
  - `/api/v2/state` 返回 `sync` 结构，包含：
    - `resources`: factionCount/autoPlayerCount/mismatchCount/entries/totals
    - `alliances`: world 指挥/指令计数、V2 同盟计数、孤儿成员计数
  - `advanceTick` 中 V2 结算结果可回写至主世界 faction 资源，避免长期漂移。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8793 npm run start` + `curl /api/v2/state` -> `PASS`
  - `npm run sim:quick` -> `EXECUTED`（exit=1，原有评测 critical gap，非本卡引入）

## Validation Update (2026-04-09, B2-C05)
- Scope: V2 Growth 最小持久化底座（避免重启后角色/同盟/保底计数归零）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `V2GameService` 增加磁盘加载与写回队列，覆盖：
    - `aiPlayers`
    - `alliances`
    - `humanPlayers`
    - `pityCounters`
  - 核心写路径（招募、升星、配队、结算、同盟变更）触发 debounce 持久化。
  - 关停流程新增 `flushV2GamePersist()`，减少最后一轮改动丢失。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8797 npm run start` + 招募/同盟相关 API 写入 + 重启后 `GET /api/v2/alliances` -> `PASS`

## Validation Update (2026-04-09, B2-C06)
- Scope: Growth 持久化可恢复性补强（legacy 兼容 + 坏文件隔离 + 加载清洗）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
- Behavior updates:
  - `hydrate` 阶段不再直接 `as` 强转，改为结构化清洗后入库，降低异常字段导致的运行时风险。
  - `buildPersistedSnapshot` 版本升级为 `V2_GAME_STATE_PERSIST_VERSION=2`。
  - 启动遇到坏 JSON 时自动隔离，确保游戏主链可继续推进。
- Validation chain:
  - `npm run build` -> `PASS`
  - legacy payload 加载 smoke -> `PASS`
  - corrupt payload 隔离 smoke -> `PASS`

## Validation Update (2026-04-09, B2-C08)
- Scope: Growth 运行态持久化健康指标接入统一 health 口径。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `/api/health.persistence.v2Game` 现在输出 `aiPlayerCount/allianceCount/humanPlayerCount` 与持久化健康指标。
  - 便于在不打开专用 debug route 的情况下直接判定 V2 持久化状态。
- Validation chain:
  - `npm run build` -> `PASS`
  - `GET /api/health` -> `PASS`（`persistence.v2Game` 字段可读）

## Validation Update (2026-04-09, B2-C09)
- Scope: Growth 侧持久化异常接入启动自检告警流（可直接用于线上巡检）。
- Changed files:
  - `server/src/app.ts`
- Behavior updates:
  - `v2Game` 的写盘失败与坏文件隔离计数参与统一告警聚合：
    - `persist_failures`（`>=5` HIGH）
    - `quarantine_detected` / `quarantine_surge`（`>=3` HIGH）
  - 告警双出口：
    - `GET /api/health.persistence.alerts`
    - 启动日志 `[startup-check][persistence][...]`
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8788 npm run start` + `GET /api/health` -> `PASS`
