# 收尾总览（B3-C17，2026-04-11）

## 0. 结论

`B3-C17` 已完成：`ai-trio-gate` 在 fork/权限受限场景会显式降级到手工粘贴路径，并把自动评论是否成功统一沉淀为可对账字段（失败摘要 + PR 模板 + CI Step Summary 同口径）。

## 1. 改动文件

1. `server/src/evals/renderGateTrioFailureSummary.ts`
2. `.github/workflows/ai-trio-gate.yml`
3. `.github/pull_request_template.md`
4. `docs/modules_v2/M18.md`
5. `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
6. `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`
7. `docs/CLOSEOUT_B3_C17_2026_04_11.md`
8. `docs/AI_ENGINEER_HUB_2026_03_25.md`

## 2. 行为变更

1. `renderGateTrioFailureSummary`：
   - 新增/固化 `prAutoCommentStatus`、`prAutoCommentReason`、`prAutoCommentOutcome` 输出。
   - 对 `fork_permission_restricted`、`manual_override`、`permission_denied` 给出降级提示与下一步动作。
2. `ai-trio-gate` workflow：
   - 自动评论结果会被 reconcile，并传回失败摘要渲染命令。
3. PR 模板：
   - `Gate Trio Failure Snapshot` 与高危自动提示块均包含自动评论对账字段，便于人工核对“评论是否真的发出”。

## 3. 验证链

1. `npm run build` -> `PASS`
2. `npm run gate:ai:nightly:acceptance` -> `PASS`（runId: `ai_nightly_2026-04-11T10-49-13-964Z`）
3. `npm run gate:ai:trio` -> `PASS`（nightly runId: `ai_nightly_2026-04-11T10-57-32-874Z`，summary runId: `gate_trio_summary_2026-04-11T10-58-09-148Z`）
4. `npm run gate:ai:trio:failure-summary -- --summary-path tmp/gates/gate-trio/gate_trio_summary_latest.json --output tmp/gates/gate-trio/gate_trio_failure_summary.md --pr-auto-prompt-output tmp/gates/gate-trio/gate_trio_pr_auto_prompt.md --ci-outcome failure --pr-auto-comment-status skipped --pr-auto-comment-reason fork_permission_restricted` -> `PASS`

## 4. 风险与边界

1. 本卡不改变 gate 通过/失败判定，仅增强失败摘要与 PR 协作对账可读性。
2. fork/权限受限时自动评论会降级为手工路径，属于预期行为，不应判定为链路故障。
3. 仍需在 PR 描述/评论区回填对账字段，避免“自动评论已发”与“实际未发”错位。

## 5. 下一步建议

1. 进入 B3-C18（docs 第二阶段精简）：把低频历史 prompt 文档统一标记为 `reference-only` 并迁移到归档区。
