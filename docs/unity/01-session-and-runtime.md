# Unity ?????Session / Auth / Runtime

????? Unity ?????????????????????????????? `server/src/routes/session.ts`?`server/src/routes/sessionBody.ts` ? `server/src/multiplayer/SessionManager.ts`?

## 1. ????

??????????

- `npm run start`????? HTTP ??
- `npm run start:clock`???? `GAME_CLOCK_ENABLED=1` ???????
- `npm run test:session:manager`????????? token ????

???????`http://127.0.0.1:8787`

## 2. Session/Auth/Runtime ??

| ?? | ?? | ?? | ?? |
|---|---|---|---|
| ???? | POST | `/api/session/join` | ?????????? session token |
| ???? | POST | `/api/session/heartbeat` | ???????????????? L1 |
| ???? | POST | `/api/session/leave` | ???? token ????? |
| ???? | GET | `/api/session/status` | ????????? AI ???? |
| ???? | GET | `/api/session/metrics` | ??????????? |
| ??? | GET | `/api/session/runtime` | ?? Unity ???????/??/?????? |
| ???? | POST | `/api/session/autonomy` | ???? L1/L2/L3 |

## 3. Token ????

### 3.1 ??

- `POST /api/session/join` ????????? `token`?
- token ?**???????**?????? **48 ??? hex**?`^[a-f0-9]{48}$`?
- ?????? JWT???????????? token?
- token ? `factionId` ???????????????????????

### 3.2 ????

- ??????????? token??????????????? token?
- `POST /api/session/heartbeat` ??? `lastHeartbeat`?
- ?????????????? L2/L3??????? `L1_assigned`?

### 3.3 ???????

- ????? `SESSION_HEARTBEAT_TIMEOUT_MS` ?????????? `L1_assigned` ???? `L2_delegated`?
- ????? `SESSION_STALE_TTL_MS` ?????????????????
- ???????????????
  - `heartbeatTimeoutMs = 30000`
  - `staleSessionTtlMs = 600000`
  - `maxActiveSessions = 1024`
  - `maxPlayerNameLength = 32`

### 3.4 ????

- `POST /api/session/leave` ????????????????
- leave ????? `ok: false`?

## 4. API ??

### 4.1 POST `/api/session/join`

????

```json
{
  "factionId": "player",
  "playerName": "Alice"
}
```

?????

```json
{
  "sessionId": "4f1f0b3d-2d0e-4cfe-9f3f-6d5eac2a8f8e",
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef",
  "factionId": "player"
}
```

?????

```json
{
  "error": "Faction player already claimed by Alice"
}
```

???????

- `factionId` ? `playerName` ????????????
- `playerName` ?? 64 ????? trim ????????
- ?? Unity-first ????? `factionId = "player"`
- ??????? `403`
- ?????? `409`

### 4.2 POST `/api/session/heartbeat`

????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef"
}
```

?????

```json
{
  "ok": true
}
```

???????

```json
{
  "ok": false,
  "error": "Invalid token"
}
```

?????

- token ??? 48 ??? hex
- ????????HTTP 400??? `{"error":"valid token required"}`
- token ????????????HTTP 200??? `{"ok":false,"error":"Invalid token"}`

### 4.3 POST `/api/session/leave`

????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef"
}
```

?????

```json
{
  "ok": true
}
```

?????

```json
{
  "ok": false
}
```

???

- token ??????HTTP 400??? `{"error":"valid token required"}`
- token ??????????HTTP 200??? `{"ok":false}`

### 4.4 GET `/api/session/status`

???????

```json
{
  "players": [
    {
      "sessionId": "4f1f0b3d-2d0e-4cfe-9f3f-6d5eac2a8f8e",
      "factionId": "player",
      "playerName": "Alice",
      "online": true,
      "autonomyLevel": "L1_assigned"
    }
  ],
  "aiControlledFactions": ["enemy", "neutral"]
}
```

?????

- `players`???????????????
- `online`???????????
- `autonomyLevel`?????????
- `aiControlledFactions`????????? AI ???????

### 4.5 GET `/api/session/metrics`

???????

```json
{
  "activeSessions": 1,
  "onlineSessions": 1,
  "delegatedSessions": 0,
  "claimedFactions": 1,
  "maxActiveSessions": 1024,
  "heartbeatTimeoutMs": 30000,
  "staleSessionTtlMs": 600000,
  "maxPlayerNameLength": 32
}
```

???

- Unity ??????????
- ????????????????????

### 4.6 GET `/api/session/runtime`

?? Unity ?????????????????????????????

???????

```json
{
  "tick": 128,
  "worldVersion": 42,
  "factions": [
    {
      "factionId": "player",
      "autonomyLevel": "L1_assigned",
      "controlMode": "human_assigned",
      "playerName": "Alice",
      "online": true,
      "doctrinePreview": "????????????...",
      "doctrineUpdatedAt": "2026-03-28T09:00:00.000Z",
      "hasModelConfig": true,
      "commanderModel": "qwen2.5-72b-instruct",
      "generalModel": "qwen2.5-14b-instruct",
      "unitModel": "qwen2.5-3b-instruct"
    }
  ]
}
```

?????

- `tick`????? Tick
- `worldVersion`????????
- `autonomyLevel`???????
- `controlMode`???????????????
- `doctrinePreview`????????????? 180 ??
- `doctrineUpdatedAt`?????????
- `hasModelConfig`????????????
- `commanderModel` / `generalModel` / `unitModel`??????

### 4.7 POST `/api/session/autonomy`

????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef",
  "level": "L2_delegated"
}
```

????

- `L1_assigned`
- `L2_delegated`
- `L3_negotiated`

?????

```json
{
  "ok": true
}
```

?? token ?????????????????

```json
{
  "ok": false,
  "error": "Invalid token"
}
```

## 5. ?????

| HTTP ??? | ???? | ???? |
|---|---|---|
| 200 | ????????????? | `{"ok":true}` / `{"ok":false,"error":"..."}` |
| 400 | ???????token ?????level ?? | `{"error":"valid token required"}` |
| 403 | Unity-first ??????? `player` ?? | `{"error":"Only faction "player" can be human-controlled in current Unity-first phase."}` |
| 404 | ??????? | `{"error":"Session route not found"}` |
| 409 | ????????????????????? | `{"error":"Faction player already claimed by Alice"}` |

## 6. L1 / L2 / L3 ??

??????????????????

### L1_assigned

- ???????????????
- Unity ?????? `human_assigned`
- ?????
  - join ????? L1
  - heartbeat ????/??? L1
- ??????????????????

### L2_delegated

- ???AI ?????
- Unity ?????? `ai_delegated`
- ?????
  - ? L1 ????????????????? L2
  - ????????????? L2
- ?????????????????

### L3_negotiated

- ???????????/???????????
- Unity ?????? `ai_negotiated`
- ?????
  - `/api/session/autonomy` ??????? L3
  - `SessionManager` ????????????? L3
- ????????????????????????????

## 7. Unity ???????

1. ?????`npm run start` ? `npm run start:clock`
2. ?? `POST /api/session/join` ?? `sessionId` ? `token`
3. ? `token` ?? Unity ?????
4. ? 10-15 ????? `POST /api/session/heartbeat`
5. UI ????? `GET /api/session/runtime`
6. ????????? `GET /api/session/status` ? `GET /api/session/metrics`
7. ??????? `POST /api/session/leave`

## 8. ??????

?????? `curl`??????????????????

```bash
npm run start
```

????????

### 8.1 ????

```powershell
@'
{"factionId":"player","playerName":"Alice"}
'@ | curl.exe -s -X POST "http://127.0.0.1:8787/api/session/join" -H "Content-Type: application/json" --data-binary @-
```

### 8.2 ????

??????? `token` ????

```powershell
@'
{"token":"0123456789abcdef0123456789abcdef0123456789abcdef"}
'@ | curl.exe -s -X POST "http://127.0.0.1:8787/api/session/heartbeat" -H "Content-Type: application/json" --data-binary @-
```

### 8.3 ?????

```powershell
curl.exe -s "http://127.0.0.1:8787/api/session/runtime"
```

### 8.4 ????????

```powershell
@'
{"token":"0123456789abcdef0123456789abcdef0123456789abcdef","level":"L2_delegated"}
'@ | curl.exe -s -X POST "http://127.0.0.1:8787/api/session/autonomy" -H "Content-Type: application/json" --data-binary @-
```

### 8.5 ????

```powershell
@'
{"token":"0123456789abcdef0123456789abcdef0123456789abcdef"}
'@ | curl.exe -s -X POST "http://127.0.0.1:8787/api/session/leave" -H "Content-Type: application/json" --data-binary @-
```

### 8.6 PowerShell ??

```powershell
$base = 'http://127.0.0.1:8787'
$join = Invoke-RestMethod -Method Post -Uri "$base/api/session/join" -ContentType 'application/json' -Body '{"factionId":"player","playerName":"Alice"}'
Invoke-RestMethod -Method Post -Uri "$base/api/session/heartbeat" -ContentType 'application/json' -Body (@{ token = $join.token } | ConvertTo-Json -Compress)
Invoke-RestMethod -Method Get -Uri "$base/api/session/runtime"
Invoke-RestMethod -Method Post -Uri "$base/api/session/autonomy" -ContentType 'application/json' -Body (@{ token = $join.token; level = 'L2_delegated' } | ConvertTo-Json -Compress)
Invoke-RestMethod -Method Post -Uri "$base/api/session/leave" -ContentType 'application/json' -Body (@{ token = $join.token } | ConvertTo-Json -Compress)
```

## 9. ????

- Session/Auth/Runtime ???????????????????
- token ????????? L1/L2/L3 ??????????
- Unity ???????? curl / PowerShell ???????
