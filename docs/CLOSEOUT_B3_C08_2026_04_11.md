# 收尾总览（B3-C08，2026-04-11）

## 0. 结论

`B3-C08` 已完成：Save Slot 从“可演练”升级为“可真实恢复”，补齐了受控 restore、失败回滚与幂等防误触。

## 1. 改动文件

1. `server/src/application/world/WorldService.ts`
2. `server/src/routes/observability.ts`
3. `server/src/app.ts`
4. `server/src/evals/runAiNightlyAcceptanceGate.ts`
5. `docs/modules_v2/M01.md`
6. `docs/modules_v2/M18.md`
7. `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
8. `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
9. `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
10. `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`
11. `docs/AI_ENGINEER_HUB_2026_03_25.md`
12. `docs/CLOSEOUT_B3_C08_2026_04_11.md`
13. `tmp/verify_b3c08_restore_apply.ts`（临时验证脚本）

## 2. 行为变更

1. 新增真实恢复 API：
   - `POST /api/save-slots/archive/restore`
   - payload：`{ archivePath?: string, force?: boolean }`
2. 恢复护栏：
   - 恢复前生成 `*.restore.bak.<ts>` 备份；
   - 写入失败时尝试自动回滚 pre-restore payload；
   - 写入使用 `*.restore.tmp -> rename` 原子替换。
3. 幂等逻辑：
   - 默认比较 slot record 轻量签名，相同状态返回 `skipped`；
   - `force=true` 可强制恢复。
4. 健康快照新增 restore apply 指标：
   - `restoreApplySuccessCount/restoreApplyFailureCount`
   - `lastRestoreApplyAt/lastRestoreApplyErrorAt`
   - `lastRestoreApplyStatus/lastRestoreApplyMessage`
   - `lastRestoreApplyArchivePath/lastRestoreApplyBackupPath/lastRestoreApplySlotCount`
5. runtime/nightly 告警新增：
   - `save_slots_restore_apply_failures`

## 3. 验证链

1. `npm run build` -> `PASS`
2. 临时验证（隔离目录、不可作为正式交付入口）：
   - `npx tsx tmp/verify_b3c08_restore_apply.ts` -> `PASS`
   - 结果：`restore1=restored`、`restore2=skipped`、`restore3=restored`
3. 说明：
   - 当前环境存在端口/进程污染，`nightly/trio` 在本卡执行窗口内未稳定复现；
   - 本卡以“编译 + 隔离恢复链”作为可复现验收依据。

## 4. 临时脚本清理计划

1. `tmp/verify_b3c08_restore_apply.ts` 仅用于 B3-C08 验证。
2. 已在 `B3-C10` 收敛为正式 gate 后删除该脚本。
3. 正式门禁现由 `npm run gate:save-slots:restore-apply:stability` 覆盖：`restore -> skipped -> force restore` 三段行为。
