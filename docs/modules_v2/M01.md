# M01 - Runtime Gateway

## Definition
- Category: Backend
- Owner AI engineer: AI-BE-EntryRuntime
- Backup AI engineer: AI-BE-RuleState
- Goal: Node HTTP bootstrap, base route dispatch, env bootstrap and request I/O safety.

## Code Boundaries
- `server/src/app.ts`
- `server/src/bootstrap/`
- `server/src/routes/http.ts`

## Official Entrypoints
- npm run server:dev
- GET /api/health

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C09)
- Scope: unify runtime/API error status semantics into three buckets (`400` / `409` / `500`) to reduce client ambiguity.
- Changed files:
  - `server/src/routes/http.ts`
- Core behavior update:
  - `writeJson` now normalizes all `>=400` statuses through a shared rule:
    - `409` stays `409` (conflict/状态竞争)
    - other `4xx` normalize to `400` (client request issue)
    - `5xx` normalize to `500` (server/internal issue)
  - `HttpBodyError` oversized-body branch is normalized as client-side invalid request (`400`).
- Validation chain (official + manual smoke):
  - `npm run build` -> `PASS`
  - `PORT=8792 npm run start` + manual route smoke:
    - `GET /api/route-not-found-smoke` -> `400`
    - `POST /api/diplomacy/propose` with invalid payload -> `400` (previously `422`)
    - `POST /api/diplomacy/respond` duplicate response -> `409`
    - `POST /api/planning/create` with forced gateway strict failure -> `500`

## Validation Update (2026-04-09, B2-C08)
- Scope: 在 runtime health 输出中增加持久化健康诊断聚合。
- Changed files:
  - `server/src/app.ts`
- Behavior updates:
  - `GET /api/health` 新增 `persistence` 区块：
    - `factionConfig`
    - `aiConfig`
    - `v2Game`
  - 每个区块提供 loaded/dirty/inFlight/成功失败计数/最近失败时间等运行态指标。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8802 npm run start` + `GET /api/health` -> `PASS`（可读到 `persistence.*` 字段）

## Validation Update (2026-04-09, B2-C09)
- Scope: 启动自检 + 持久化分级告警（值班排障入口）。
- Changed files:
  - `server/src/app.ts`
- Behavior updates:
  - 新增 `persistence.alerts[]`（`severity/source/code/message`）并随 `GET /api/health` 返回。
  - 服务启动后自动执行一次持久化自检：
    - 无告警输出 `[startup-check] persistence health: ok`
    - 有告警输出 `[startup-check][persistence][SEVERITY][source][code] ...`
  - 当前告警规则覆盖：
    - `missing_encryption_key`（HIGH）
    - `plaintext_persist_enabled`（MEDIUM）
    - `persist_failures`（MEDIUM/HIGH）
    - `quarantine_detected` / `quarantine_surge`（MEDIUM/HIGH）
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8788 npm run start` + `GET /api/health` -> `PASS`（存在 `persistence.alerts`）
  - 启动日志检查 -> `PASS`（观测到 `HIGH missing_encryption_key` 分级输出）

## Validation Update (2026-04-09, B2-C11)
- Scope: Save Slot 最小持久化（重启可恢复）并接入关停 flush。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - Save Slot 从纯内存升级为磁盘持久化：
    - 默认路径：`tmp/world_save_slots.json`
    - 可通过 `WORLD_SAVE_SLOTS_PATH` 覆盖
  - 启动阶段自动恢复 Save Slot；支持 legacy array 与 envelope 结构读取。
  - 写入采用 debounce + `.tmp` 原子替换；解析失败时隔离为 `*.corrupt.<ts>`。
  - 关停链路新增 `flushSaveSlotsPersist()`，降低最后一轮保存丢失概率。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8796/WORLD_SAVE_SLOTS_PATH=tmp/world_save_slots_smoke_restore.json`：
    - `POST /api/save-slots/save` -> `PASS`
    - 重启到 `PORT=8797` 后 `GET /api/save-slots` 仍可读到同一 slot -> `PASS`

## Validation Update (2026-04-09, B2-C12)
- Scope: 持久化告警 runbook 固化（统一值班口径）。
- Changed files:
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
  - `docs/modules_v2/M01.md`
- Behavior updates:
  - 明确了 `persistence.alerts` 的告警码、阈值、处置优先级（P1/P2/P3）。
  - 固化了值班流程：T+0 分级、T+10 定位、T+30 处置/升级。
  - 固化正式验证链（`npm run build` + `GET /api/health`）与边界声明。
- Validation chain:
  - `obsidian files folder='docs' ext=md` -> `PASS`
  - `GET /api/health` 告警字段抽样核对 -> `PASS`
  - UTF-8 回读校验 -> `PASS`

## Validation Update (2026-04-10, B2-C15)
- Scope: Runtime health 聚合扩展 Session 持久化健康与告警来源。
- Changed files:
  - `server/src/app.ts`
  - `server/src/multiplayer/SessionManager.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
- Behavior updates:
  - `GET /api/health.persistence` 新增 `session` 区块（`path/enabled/loaded/sessionCount/persist*/quarantine*/restored*`）。
  - `persistence.alerts` 规则新增 `source=session` 的 `persist_failures` 与 `quarantine_detected/surge` 聚合。
  - 夜间门禁 `gate:ai:nightly:acceptance` 的 `persistence_snapshot_available` 已纳入 `sessionPath` 校验。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（可见 `persistence.session.path`）
  - `PORT=8811 + GET /api/health` -> `PASS`（`persistence.session` 字段可读）

## Validation Update (2026-04-10, B3-C03)
- Scope: Save Slot 迁移策略最小卡（版本演进 + 跨环境迁移口径）。
- Changed files:
  - `docs/modules_v2/M01.md`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
  - `docs/PROJECT_EVOLUTION_NARRATIVE_2026_04_09.md`
- Behavior updates:
  - 固化 Save Slot 持久化契约（当前 v1）：
    - 文件路径默认 `tmp/world_save_slots.json`（可由 `WORLD_SAVE_SLOTS_PATH` 覆盖）。
    - 推荐 envelope 结构：`{ version, savedAt, slots[] }`。
    - 读取兼容保留 `legacy array` + `envelope` 双通道。
  - 固化版本演进约束：
    - 若 `slots` 结构发生 breaking change，必须同步提升 `SAVE_SLOTS_PERSIST_VERSION`。
    - `extractPersistedSaveSlots` 必须保留向后兼容（至少覆盖 `legacy array + v1 envelope`）。
    - 升级后必须通过 `fixture_slot_primed/loaded/restored` 与 `template_steps_all_ok` 验证链。
  - 固化跨环境迁移最小流程：
    - 先停服/flush，再备份并复制 save-slot 文件到目标环境路径。
    - 目标环境启动后执行 `GET /api/save-slots` 与 `npm run gate:ai:nightly:acceptance` 复核。
  - 边界声明：
    - `saveSlots` 尚未接入 `health.persistence.alerts.source`，当前仍以 gate + 人工巡检兜底。
- Validation chain:
  - 文档 UTF-8 回读：`PASS`
  - `npm run gate:ai:nightly:acceptance`：`PASS`

## Validation Update (2026-04-10, B3-C04)
- Scope: Save Slot 健康纳管（runtime + nightly + alerts source）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/modules_v2/M01.md`
  - `docs/modules_v2/M18.md`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - 新增 `getSaveSlotsPersistHealth()`，`/api/health.persistence` 增加 `saveSlots` 区块：
    - `path/loaded/slotCount/persistDirty/persistInFlight`
    - `persistSuccessCount/persistFailureCount/lastPersistAt/lastPersistErrorAt`
    - `corruptQuarantineCount/lastCorruptQuarantineAt`
    - `restoredSlotCount/lastRestoreAt/persistVersion`
  - `persistence.alerts` 聚合规则扩展 `source=saveSlots`（`persist_failures` 与 `quarantine_detected/surge`）。
  - nightly `persistence_snapshot_available` 现要求 `saveSlotsPath`，并输出到 report `details`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（报告可见 `persistence.saveSlots` + `saveSlotsPath`）

## Validation Update (2026-04-10, B3-C05)
- Scope: Save Slot 大文件治理（soft/hard 阈值 + 告警/门禁）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/modules_v2/M01.md`
  - `docs/modules_v2/M18.md`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - Save Slot 健康快照新增文件体积字段：
    - `fileSizeBytes`
    - `fileSizeLevel`（`none|ok|soft|hard`）
    - `softLimitBytes`
    - `hardLimitBytes`
  - 默认阈值（可用 env 覆盖）：
    - `WORLD_SAVE_SLOTS_SOFT_LIMIT_BYTES=134217728`（128 MiB）
    - `WORLD_SAVE_SLOTS_HARD_LIMIT_BYTES=536870912`（512 MiB）
  - `persistence.alerts` 新增 Save Slot 体积告警：
    - `save_slots_oversize_soft`（MEDIUM）
    - `save_slots_oversize_hard`（HIGH）
  - startup 自检会直接输出 Save Slot 体积中/高告警，便于值班提前发现大文件风险。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（可见 `save_slots_not_hard_oversize=true` 与 `alerts[source=saveSlots]`）

## Validation Update (2026-04-10, B3-C06)
- Scope: Save Slot 归档压缩 + 多实例写锁治理（runtime + nightly）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - Save Slot 健康快照新增归档与锁治理指标：
    - `archiveOnSoftLimit/archiveDir/archiveMaxFiles`
    - `archiveFileCount/archiveSuccessCount/archiveFailureCount`
    - `lastArchiveAt/lastArchiveErrorAt/lastArchivePath`
    - `lockPath/lockStaleMs/lockContentionCount/lockStealCount/lockFailureCount`
  - soft 超限（`fileSizeBytes >= softLimitBytes`）时自动执行 gzip 归档（默认开启）：
    - 默认目录 `tmp/world_save_slots_archive`
    - 默认保留 `12` 份，超出自动清理旧归档
  - Save Slot 持久化写入新增锁文件治理：
    - 锁路径：`<saveSlotsPath>.lock`
    - stale 锁阈值：`WORLD_SAVE_SLOTS_LOCK_STALE_MS`（默认 15000ms）
    - stale 锁会被隔离为 `*.stale.<ts>` 并重试一次
  - 新增环境变量：
    - `WORLD_SAVE_SLOTS_ARCHIVE_ON_SOFT_LIMIT`（默认 `true`）
    - `WORLD_SAVE_SLOTS_ARCHIVE_DIR`（默认 `tmp/world_save_slots_archive`）
    - `WORLD_SAVE_SLOTS_ARCHIVE_MAX_FILES`（默认 `12`）
    - `WORLD_SAVE_SLOTS_LOCK_STALE_MS`（默认 `15000`）
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - API 实测：`POST /api/save-slots/save` 后 `GET /api/health` 可见 `archiveSuccessCount` 增长

## Validation Update (2026-04-11, B3-C07)
- Scope: Save Slot 归档恢复演练（archive catalog + dry-run drill + gate）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - 新增 Save Slot 归档目录正式查询入口：
    - `GET /api/save-slots/archive` -> `archives[]`（`path/sizeBytes/modifiedAt`）
  - 新增 Save Slot 归档恢复演练入口（非破坏）：
    - `POST /api/save-slots/archive/restore-drill`
    - 对最新或指定 `.json.gz` 归档执行 dry-run 解压/解析/slot 结构校验，不改 active store
  - Save Slot 健康快照新增恢复演练指标：
    - `restoreDrillSuccessCount/restoreDrillFailureCount`
    - `lastRestoreDrillAt/lastRestoreDrillErrorAt`
    - `lastRestoreDrillArchivePath/lastRestoreDrillSlotCount`
    - `lastRestoreDrillStatus/lastRestoreDrillMessage`
  - runtime 告警新增：
    - `save_slots_restore_drill_failures`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - API 实测：
    - `GET /api/save-slots/archive` -> `PASS`
    - `POST /api/save-slots/archive/restore-drill` -> `PASS`
    - `GET /api/health` 可见 `restoreDrillSuccessCount` 增长 -> `PASS`

## Validation Update (2026-04-11, B3-C08)
- Scope: Save Slot 真实恢复入口（受控 restore + rollback + 幂等跳过）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - 新增 Save Slot 真实恢复入口：
    - `POST /api/save-slots/archive/restore`
    - payload：`{ archivePath?: string, force?: boolean }`
  - 恢复流程新增回滚护栏：
    - 恢复前落盘备份：`<saveSlotsPath>.restore.bak.<ts>`
    - 失败时尝试回滚 pre-restore payload，防止 active store 被半写污染
  - 幂等防误触：
    - 默认按 slot record 轻量签名比对，重复恢复同一状态会返回 `skipped`
    - `force=true` 可强制重放
  - Save Slot 健康快照新增真实恢复指标：
    - `restoreApplySuccessCount/restoreApplyFailureCount`
    - `lastRestoreApplyAt/lastRestoreApplyErrorAt`
    - `lastRestoreApplyStatus/lastRestoreApplyMessage`
    - `lastRestoreApplyArchivePath/lastRestoreApplyBackupPath/lastRestoreApplySlotCount`
  - runtime/nightly 告警新增：
    - `save_slots_restore_apply_failures`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npx tsx tmp/verify_b3c08_restore_apply.ts`（隔离目录临时验证）-> `PASS`
    - `restore1=restored`
    - `restore2=skipped`
    - `restore3=restored`

## Validation Update (2026-04-11, B3-C09)
- Scope: Save Slot restore apply 门禁化（无副作用可观测 + rollback drill gate）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
- Behavior updates:
  - 新增 Save Slot 回滚演练入口（正式 API）：
    - `POST /api/save-slots/archive/restore-rollback-drill`
    - 对归档恢复执行“模拟失败后回滚验证”，落在独立 drill store，不修改 active save-store
  - Save Slot 健康快照新增 rollback drill 指标：
    - `restoreRollbackDrillSuccessCount/restoreRollbackDrillFailureCount`
    - `lastRestoreRollbackDrillAt/lastRestoreRollbackDrillErrorAt`
    - `lastRestoreRollbackDrillStatus/lastRestoreRollbackDrillMessage`
    - `lastRestoreRollbackDrillArchivePath/lastRestoreRollbackDrillBackupPath`
    - `lastRestoreRollbackDrillStorePath/lastRestoreRollbackDrillSlotCount/lastRestoreRollbackDrillVerified`
  - runtime/nightly 告警新增：
    - `save_slots_restore_rollback_drill_failures`
  - nightly 新增 restore apply 门禁检查：
    - `save_slots_restore_apply_metrics_observable`
    - `save_slots_restore_apply_failure_rollback_trace`
    - `save_slots_archive_restore_rollback_drill_passed`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`
  - API 实测：
    - `POST /api/save-slots/archive/restore-rollback-drill` -> `PASS`（`status=passed`）
    - `GET /api/health` 可见 `restoreRollbackDrillSuccessCount` 增长 -> `PASS`

## Validation Update (2026-04-11, B3-C10)
- Scope: Save Slot restore apply 正式 gate 收敛（三段行为 + 临时脚本清理）。
- Changed files:
  - `server/src/evals/runSaveSlotsRestoreApplyGate.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `package.json`
  - `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
- Behavior updates:
  - 新增正式门禁入口：
    - `npm run gate:save-slots:restore-apply:stability`
  - gate 在隔离 workspace 中强制覆盖三段行为：
    - `restored -> skipped -> force restored`
  - nightly 新增 restore apply gate 三项检查：
    - `save_slots_restore_apply_gate_exit_zero`
    - `save_slots_restore_apply_gate_report_parsed`
    - `save_slots_restore_apply_gate_passed`
  - 已删除临时验证脚本：
    - `tmp/verify_b3c08_restore_apply.ts`
- Validation chain:
  - `npm run gate:save-slots:restore-apply:stability` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-11, B3-C11)
- Scope: Save Slot restore apply gate 产物保留策略（latest + 最近 N 份）与 trio summary 关键结果并入。
- Changed files:
  - `server/src/evals/runSaveSlotsRestoreApplyGate.ts`
  - `server/src/evals/runGateTrioSummary.ts`
  - `docs/modules_v2/M01.md`
  - `docs/modules_v2/M18.md`
- Behavior updates:
  - `save-slots-restore-apply-gate` 报告落盘新增保留策略：
    - 固定保留 `save_slots_restore_apply_gate_latest.json`
    - 额外保留最近 `N` 份 stamped 报告与对应隔离 workspace
    - 超出 `N` 的历史报告/workspace 自动清理
    - workspace 清理仅针对“已淘汰 stamped 报告对应 runId”，避免并发 gate 误删在跑目录
  - 新增环境变量：
    - `SAVE_SLOTS_RESTORE_APPLY_GATE_KEEP_RECENT`（默认 `12`，最小 `1`）
  - restore apply gate latest 报告新增 `artifacts` 字段，记录：
    - `keepRecent/retainedRunIds/prunedRunIds/prunedReports/prunedWorkspaces`
  - trio summary 新增 `reports.saveSlotsRestoreApply` 节点，直接汇总 restore apply gate 关键结果：
    - 三段状态（`first/second/third`）
    - 健康计数（`restoreApplySuccessCount/restoreApplyFailureCount`）
    - 产物保留统计（`retainedCount/prunedCount`）
  - trio summary 新增三项显式检查：
    - `save_slots_restore_apply_gate_passed`
    - `nightly_references_latest_restore_apply_gate`
    - `save_slots_restore_apply_three_phase_semantics`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`
