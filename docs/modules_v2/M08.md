# M08 - Session Autonomy Doctrine

## Definition
- Category: Backend
- Owner AI engineer: AI-Agent-GovDiplo
- Backup AI engineer: AI-BE-EntryRuntime
- Goal: L1/L2 autonomy switching, player session lifecycle, doctrine/model overrides and auto tick.

## Code Boundaries
- `server/src/multiplayer/`
- `server/src/application/clock/`
- `server/src/application/faction/`
- `server/src/routes/session.ts`
- `server/src/routes/factionConfigRoutes.ts`

## Official Entrypoints
- npm run start:clock
- POST /api/session/join|heartbeat|leave
- GET /api/session/status

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C06)
- Scope: Session 路由输出与事件回放统一带 `autonomyLevel/controlMode`，增强 L1/L2/L3 状态可解释性。
- Changed:
  - `server/src/multiplayer/SessionManager.ts`
  - `server/src/routes/session.ts`
  - `server/src/application/world/WorldService.ts`
- Validation chain:
  - `npm run build` (PASS)
  - `npm run test:session:manager` (PASS)
  - `GET /api/session/runtime` + `POST /api/session/join` + `POST /api/session/autonomy` + `GET /api/events?limit=20` (PASS)
- Expected:
  - `session_join/session_heartbeat/session_set_autonomy/session_leave` 均可输出当前或过渡后的控制态信息。
  - 客户端可直接按 `controlMode` 做展示，不需要自行推断 autonomy 语义。

## Validation Update (2026-04-09, B2-C05)
- Scope: 配置层最小持久化（Faction doctrine/model）并接入进程关停 flush。
- Changed:
  - `server/src/application/faction/FactionConfigStore.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `FactionConfigStore` 从纯内存改为磁盘持久化（默认 `tmp/faction_configs.json`，支持 `FACTION_CONFIG_STORE_PATH` 覆盖）。
  - doctrine/model 更新改为 debounce + 原子写（`*.tmp` rename）并在 `SIGINT/SIGTERM` 时强制 flush。
  - 保留原有读取/写入接口，不改路由协议，仅增强重启恢复能力。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8797 npm run start` + `POST /api/faction/wei/doctrine` + 重启后 `GET /api/faction/wei/config` -> `PASS`

## Validation Update (2026-04-09, B2-C06)
- Scope: Faction 配置持久化鲁棒性补强（版本封装 + 旧格式兼容 + 损坏文件隔离）。
- Changed:
  - `server/src/application/faction/FactionConfigStore.ts`
- Behavior updates:
  - 新格式由纯数组升级为 envelope：`{ version, savedAt, items }`。
  - 兼容读取旧格式数组（legacy），无需一次性迁移脚本。
  - 加载失败时自动将坏文件重命名为 `*.corrupt.<ts>` 并回退内存空态，避免反复污染启动流程。
- Validation chain:
  - `npm run build` -> `PASS`
  - legacy array 文件加载 smoke -> `PASS`
  - corrupt JSON 文件隔离 smoke -> `PASS`

## Validation Update (2026-04-09, B2-C07)
- Scope: BYOK `apiKey` 持久化安全化（加密落盘 + 默认禁明文持久化）。
- Changed:
  - `server/src/application/faction/FactionConfigStore.ts`
- Behavior updates:
  - 新增 `FACTION_APIKEY_ENCRYPTION_KEY`：存在时以 AES-256-GCM 加密 `apiKey` 后落盘。
  - 默认不允许明文持久化 `apiKey`；仅当 `FACTION_APIKEY_ALLOW_PLAINTEXT_PERSIST=1` 才回退明文。
  - 加载时支持解密；若密钥缺失或错误，则安全降级为“丢弃 apiKey”，避免错误解密污染。
- Validation chain:
  - `npm run build` -> `PASS`
  - `POST /api/faction/wei/model-config(apiKey=...)` 后检查持久化文件不含明文 -> `PASS`
  - 同密钥重启后 `GET /api/faction/wei/config` 返回 `hasApiKey=true` -> `PASS`
  - 错误密钥重启后 `hasApiKey=false`（安全降级）-> `PASS`

## Validation Update (2026-04-09, B2-C08)
- Scope: Faction 配置持久化健康指标输出（供 runtime health 聚合）。
- Changed:
  - `server/src/application/faction/FactionConfigStore.ts`
- Behavior updates:
  - 新增 `getFactionConfigPersistHealth()`：
    - `persistSuccessCount/persistFailureCount`
    - `lastPersistAt/lastPersistErrorAt`
    - `corruptQuarantineCount/lastCorruptQuarantineAt`
    - `security.secretPersistMode`（`encrypted/plaintext/memory_only`）
- Validation chain:
  - `npm run build` -> `PASS`
  - `GET /api/health` -> `PASS`（`persistence.factionConfig.security.secretPersistMode` 可见）

## Validation Update (2026-04-09, B2-C09)
- Scope: 基于 factionConfig 健康指标的启动分级告警接入（runbook 可执行）。
- Changed:
  - `server/src/app.ts`（聚合/分级/启动输出）
- Behavior updates:
  - 当 `persistence.factionConfig.security.secretPersistMode=memory_only` 时，触发：
    - `HIGH / missing_encryption_key`
  - 当 `persistence.factionConfig.security.allowPlaintextPersist=true` 时，触发：
    - `MEDIUM / plaintext_persist_enabled`
  - 当 factionConfig 写盘失败或隔离次数增长时，触发：
    - `persist_failures`（`>=5` 为 HIGH，否则 MEDIUM）
    - `quarantine_detected` / `quarantine_surge`（`>=3` 为 HIGH）
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8788 npm run start` + `GET /api/health` -> `PASS`（`persistence.alerts` 包含 factionConfig 告警）

## Validation Update (2026-04-10, B2-C15)
- Scope: Session 在线态最小持久化与重启恢复（先定义边界再落地）。
- Changed:
  - `server/src/multiplayer/SessionManager.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
- Recovery boundary:
  - 持久化 `sessionId/token/factionId/seatId/playerName/lastHeartbeat/autonomyLevel` 到 `tmp/session_state.json`（支持 `SESSION_STATE_PERSIST_PATH` 覆盖）。
  - 重启后按心跳窗口重新判定在线态；允许客户端使用原 token 继续 heartbeat/leave。
  - 仍保持轻鉴权模型，不引入跨端强鉴权升级（该项留给后续卡）。
- Behavior updates:
  - Session 生命周期事件（join/heartbeat/autonomy/leave/sweep）触发 debounce + 原子写持久化。
  - 关停链路新增 `flushSessionPersist()`，纳入统一 flush。
  - `/api/health.persistence.session` 新增 Session 持久化健康指标（success/failure/quarantine/restored/path）。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run test:session:manager` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（`persistence.session.path` 可见）
  - `PORT=8810/8811 + SESSION_STATE_PERSIST_PATH=tmp/session_state_b2c15_smoke.json`：
    - `join + set_autonomy -> restart -> runtime/heartbeat` smoke -> `PASS`

## Validation Update (2026-04-10, B2-C16)
- Scope: Session 鉴权与 token 生命周期治理（在 B2-C15 在线态持久化基础上补安全边界）。
- Changed:
  - `server/src/multiplayer/SessionManager.ts`
  - `server/src/routes/session.ts`
  - `server/src/routes/http.ts`
  - `server/tests/session_manager.test.ts`
- Behavior updates:
  - Session 新增 token 生命周期字段：`tokenIssuedAt/tokenExpiresAt`，支持 `SESSION_TOKEN_MAX_AGE_MS`（默认 7 天，最小 60 秒）。
  - token 校验统一返回 `errorCode`：`invalid_token_format | invalid_token | token_expired`。
  - heartbeat/autonomy/leave mutation 响应新增 `tokenState`（issued/expires/remaining/expired）。
  - Session mutation 的 HTTP 语义收敛：无效或过期 token 返回 `401`（而非之前被归一化成 `400`）。
  - sweep 增加 token 过期剔除，避免过期 token 持续影响 faction 控制态。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run test:session:manager` -> `PASS`
  - `PORT=8796 SESSION_TOKEN_MAX_AGE_MS=60000 npm run start` + join/runtime/autonomy/heartbeat/leave security smoke -> `PASS`（`tmp/gates/session_security_smoke_latest.json`）
