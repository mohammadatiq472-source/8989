# 收尾总览（B3-C07，2026-04-11）

## 0. 结论

`B3-C07` 已完成：Save Slot 从“有归档文件”升级为“可枚举 + 可演练 + 可门禁”的恢复可用性闭环。

## 1. 改动文件

1. `server/src/application/world/WorldService.ts`
2. `server/src/routes/observability.ts`
3. `server/src/app.ts`
4. `server/src/evals/runAiNightlyAcceptanceGate.ts`
5. `docs/modules_v2/M01.md`
6. `docs/modules_v2/M18.md`
7. `docs/PERSISTENCE_ALERT_RUNBOOK_2026_04_09.md`
8. `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
9. `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
10. `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`
11. `docs/AI_ENGINEER_HUB_2026_03_25.md`
12. `docs/CLOSEOUT_B3_C07_2026_04_11.md`

## 2. 行为变更

1. 新增归档目录查询入口：
   - `GET /api/save-slots/archive`
   - 返回 `archives[]`（含 `path/sizeBytes/modifiedAt`）
2. 新增恢复演练入口（不改 active save-store）：
   - `POST /api/save-slots/archive/restore-drill`
   - 执行 `.json.gz` dry-run 解析与 payload/slot 校验
   - 失败返回 `422`，成功或跳过返回 `200`
3. Save Slot 健康快照新增 restore drill 指标：
   - `restoreDrillSuccessCount`
   - `restoreDrillFailureCount`
   - `lastRestoreDrillAt`
   - `lastRestoreDrillErrorAt`
   - `lastRestoreDrillArchivePath`
   - `lastRestoreDrillSlotCount`
   - `lastRestoreDrillStatus`
   - `lastRestoreDrillMessage`
4. runtime/nightly 告警新增：
   - `save_slots_restore_drill_failures`
5. nightly 新增检查：
   - `save_slots_archive_restore_drill_passed`（仅当 drill `status=failed` 时失败）

## 3. 验证链

1. `npm run build` -> `PASS`
2. `npm run gate:ai:nightly:acceptance` -> `PASS`（runId: `ai_nightly_2026-04-11T03-56-21-806Z`）
3. `npm run gate:ai:trio` -> `PASS`（nightly runId: `ai_nightly_2026-04-11T04-02-58-185Z`，summary runId: `gate_trio_summary_2026-04-11T04-03-07-682Z`）
4. API 正式入口实测：
   - `GET /api/save-slots/archive` -> `PASS`
   - `POST /api/save-slots/archive/restore-drill` -> `PASS`
5. runtime 快照字段实测：
   - `GET /api/health` 中 `persistence.saveSlots.lastRestoreDrillStatus=passed`
   - `restoreDrillSuccessCount` 增长
   - `lastRestoreDrillArchivePath/lastRestoreDrillSlotCount` 可读

## 4. 残余边界

1. 当前是 dry-run 演练，不会把归档自动回灌成 active `world_save_slots.json`。
2. 下一卡建议 `B3-C08`：增加“受控真实恢复入口 + 回滚保护 + 幂等防误触”。
