# 收尾总览（B2-C20，2026-04-10）

## 0. 结论

`B2-C20` 已完成：模板动作链路从“可执行”升级为“可回归、可对账、可落盘”。

---

## 1. 代码改动

1. CLI 剧本回放入口：
   - `godot-client/tools/slg_ops_cli.py`
   - 新增 `template-replay` 子命令（`baseline_v1`）
2. 主线门禁接入回放对账：
   - `server/src/evals/runAiMainlineStabilityGate.ts`
   - 新增 `template_replay_passed` 检查与 `templateReplay` 报告索引
3. 文档同步：
   - `docs/modules_v2/M12.md`
   - `docs/modules_v2/M15.md`
   - `docs/modules_v2/M18.md`
   - `docs/TASK_2026_04_09_AI_WEEK2_AUTONOMOUS_EXEC_CARDS.md`
   - `docs/AI_NATIVE_SLG_ALIGNMENT_AUDIT_2026_04_10.md`
   - `docs/AI_ENGINEER_HUB_2026_03_25.md`
   - `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`

---

## 2. 核心产物

1. 主 gate 回放报告：
   - `tmp/gates/ai_ops_template_replay_latest.json`
   - `tmp/gates/ai-ops-template-replay/<runId>.json`
2. CLI 独立回放报告（可复现）：
   - `tmp/gates/ai_ops_template_replay_cli_latest.json`

说明：

- `baseline_v1` 剧本包含：`clear_plan_execution`、`preview_*`、`tactical_override_first_unit`、`move_first_unit`、`advance_tick`。
- `move_first_unit` 在当时版本设为可选成功步骤（资源不足时不误报），后续已在 `B2-C21` 收紧为必选步骤。

---

## 3. 正式验证链

1. `py -3.11 -m py_compile godot-client/tools/slg_ops_cli.py`
2. `npm run build`
3. `npm run gate:ai:mainline:stability`
4. `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 world-action-template --template advance_tick`
5. `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 template-replay --scenario baseline_v1 --report-path tmp/gates/ai_ops_template_replay_cli_latest.json`
6. `GET /api/events?limit=30` + `GET /api/narratives?limit=20`

---

## 4. 下一步建议

进入 Batch2 收尾复核：连续执行 nightly/mainline/template-replay 回归，确认波动区间后再发下一批卡片。
