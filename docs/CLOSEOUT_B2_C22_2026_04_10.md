# 收尾总览（B2-C22，2026-04-10）

## 0. 结论

`B2-C22` 已完成：nightly 汇总门禁已并入“固定初始态模板回放”的显式断言，不再仅依赖 mainline 退出码间接判断。

---

## 1. 代码改动

1. nightly gate 增强：
   - `server/src/evals/runAiNightlyAcceptanceGate.ts`
2. 新增能力：
   - 读取 `tmp/gates/ai_ops_template_replay_latest.json`
   - 输出 `templateReplayGate` 节点（runId/passed/fixtureChecksPassed/allStepsRequired/requiredFailedSteps）
   - 新增 4 条检查：
     - `template_replay_report_parsed`
     - `template_replay_passed`
     - `template_replay_fixture_checks_passed`
     - `template_replay_all_steps_required`

---

## 2. 验证结果（实跑）

1. `npm run build` -> `PASS`
2. `npm run gate:ai:nightly:acceptance` -> `PASS`
3. nightly 报告检查：
   - `tmp/gates/ai-nightly-acceptance/ai_nightly_acceptance_latest.json`
   - `templateReplayGate.fixtureChecksPassed = true`
   - `templateReplayGate.allStepsRequired = true`
   - `templateReplayGate.requiredFailedSteps = []`

---

## 3. 当前三件套门禁

1. `npm run gate:session:security`
2. `npm run gate:ai:mainline:stability`
3. `npm run gate:ai:nightly:acceptance`（现已包含 fixture template-replay 显式断言）

---

## 4. 后续建议

下一步可进入 Batch2 最终归档卡：把三件套门禁与报告路径沉淀到统一“交接最短路径”文档，减少新代理接手成本。
