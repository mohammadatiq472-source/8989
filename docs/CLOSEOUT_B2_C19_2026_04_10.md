# 收尾总览（B2-C19，2026-04-10）

## 0. 结论

`B2-C19` 已完成：操作面从“可执行链”扩展到“常用动作模板链”。

---

## 1. 代码改动

1. MCP 模板工具：
   - `server/src/mcp/gameServer.ts`
   - 新增 `list_world_action_templates`、`run_world_action_template`
2. CLI 模板执行：
   - `godot-client/tools/slg_ops_cli.py`
   - 新增 `world-action-templates`、`world-action-template` 子命令
3. Godot Runtime 面板对账：
   - `godot-client/scripts/app/main.gd`
   - `godot-client/scripts/ui/observability_panel.gd`
   - `godot-client/README.md`

---

## 2. 模板能力

当前模板覆盖：

1. `advance_tick`
2. `clear_plan_execution`
3. `preview_national_agenda`
4. `preview_court_session`
5. `move_first_unit`
6. `upgrade_first_city`
7. `tactical_override_first_unit`

说明：

- move/override 模板支持自动解析 `factionId/unitId/targetTileId`（含 `map-layout` fallback）。
- Godot Runtime 观测新增 `lastAction/status/tick`，便于动作与事件回放对账。

---

## 3. 正式验证链

1. `npm run build`
2. `npm run godot:ops:cli -- world-action-templates`
3. `npm run godot:ops:cli -- world-action-template --template advance_tick --include-world`
4. `npm run godot:ops:cli -- world-action-template --template move_first_unit --include-world`
5. `npm run godot:ops:cli -- world-action --action advanceTick`
6. `GET /api/events?limit=30`（回放检查包含 `advance_tick/move_unit`）
7. `npm run gate:godot:week1`

---

## 4. 下一默认推进

建议进入 `B2-C20`：模板动作剧本化回归（events/narratives 自动对账 + JSON 报告）。
