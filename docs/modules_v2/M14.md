# M14 - Shared Contracts Schemas Domain

## Definition
- Category: Shared
- Owner AI engineer: AI-Shared-Contracts
- Backup AI engineer: AI-QA-Gates
- Goal: Single source of truth for contracts, schema validation, and domain algorithms.

## Code Boundaries
- `shared/contracts/`
- `shared/schemas/`
- `shared/domain/`

## Official Entrypoints
- npm run lint
- npm run build

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build`
- Lint (global): `PASS`
- Build: `PASS` (`vite build` completed; only bundle-size warnings)
- Module change set: `shared/contracts/game.ts` facade re-export + domain subfiles `shared/contracts/game/common.ts`, `shared/contracts/game/planning.ts`, `shared/contracts/game/history.ts`, `shared/contracts/game/ai.ts`, `shared/contracts/game/meta.ts`, `shared/contracts/game/world.ts`
- Compatibility note: root `shared/contracts/game.ts` export surface remains stable for existing callers
- Snapshot updated at: `2026-03-26 21:35 CST`

## Validation Update (2026-04-09, W2-C07)
- Scope: 为 W2 新增字段补齐 contract/schema（`wsStats`、session mutation `controlMode` 等）并在路由执行 parse 收敛。
- Changed:
  - `shared/contracts/game/history.ts`
  - `shared/contracts/game/session.ts`
  - `shared/schemas/history.ts` (new)
  - `shared/schemas/session.ts` (new)
  - `server/src/routes/observability.ts`
  - `server/src/routes/session.ts`
- Validation chain:
  - `npm run lint` (FAIL，仓库外部文件 `.obsidian/plugins/claudian/main.js` 的既有规则缺失，非本次改动引入)
  - `npm run build` (PASS)
  - `npm run test:session:manager` (PASS)
  - `session runtime/join/autonomy + /api/events` smoke (PASS)
- Compatibility note:
  - 新增字段均为向后兼容扩展（新增可选或新增响应属性），未移除旧字段。

## Validation Update (2026-04-10, B2-C16)
- Scope: Session 鉴权/生命周期字段补齐 contract+schema，避免后端新增安全字段后前后端漂移。
- Changed:
  - `shared/contracts/game/session.ts`
  - `shared/schemas/session.ts`
  - `server/src/routes/session.ts`
- Contract/schema updates:
  - 新增 `SessionAuthErrorCode`：`invalid_token_format | invalid_token | token_expired`
  - 新增 `SessionTokenState`：`issuedAt/expiresAt/remainingMs/expired`
  - `SessionJoinResponse` 增加可选 `tokenState`
  - `SessionMutationResponse` 增加 `errorCode` 与可选 `tokenState`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run test:session:manager` -> `PASS`
  - `PORT=8796` session security smoke（join/runtime/autonomy/heartbeat/leave）-> `PASS`
- Compatibility note:
  - 仅新增可选字段与错误码，不移除原字段；旧客户端仍可按 `ok/error` 语义工作。
