# M18 - Evals Tests and Batch Orchestrator

## Definition
- Category: QA
- Owner AI engineer: AI-QA-Gates
- Backup AI engineer: AI-QA-Gates
- Goal: Official simulation/eval/test pipelines and batch orchestrator quality gates.

## Code Boundaries
- `server/src/evals/`
- `server/evals/`
- `server/tests/`
- `scripts/`
- `server/src/agents/orchestrator/`

## Official Entrypoints
- npm run eval:planning:offline
- npm run eval:orchestrator:stress
- npm run gate:phase5:hardening
- npm run gate:harness:isolation
- npm run gate:gateway:preflight
- npm run gate:scale:3000:mock
- npm run gate:scale:3000:gateway
- npm run gate:scale:3000:gateway:ready
- npm run test:session:manager
- npm run sim:13factions

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run test:world:mutation-lock` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `PASS`
- Build: `PASS` (`vite build` completed; bundle-size warnings only)
- World lock test: `PASS` (`[world_mutation_lock] all checks passed`)
- Eval planning offline: `PASS` (`fullPassRate=0.8`, `tacticalReasonableRate=0.8`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`passed=true`)
- Snapshot updated at: `2026-03-26 22:09 CST`

## Validation Snapshot (2026-03-27)
- Validation chain: `npm run gate:harness:isolation` -> `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening` -> `npm run gate:scale:3000:mock`
- Harness isolation: `PASS`
- Lint (global): `PASS`
- Build: `PASS`
- Eval planning offline: `PASS` (`fullPassRate=0.8`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `totalAgents=24`)
- Gate phase5 hardening: `PASS`
- Gate scale 3000 mock: `PASS` (`successRate=1`, `fallbackRate=0`, `failureCount=0`, `p95LatencyMs=156908`)
- Gateway smoke (24 agents): `FAIL` (`successRate=0`, `fallbackRate=1`, blocker=`missing env LLM_RELAY_URL`)
- Gateway preflight gate: `ADDED` (`npm run gate:gateway:preflight`)
- Snapshot updated at: `2026-03-27 00:36 CST`

## Validation Snapshot (2026-03-27, Session Boundary Hardening)
- Validation chain: `npm run test:session:manager` -> `npm run test:world:mutation-lock` -> `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening` -> `npm run gate:scale:3000:mock` -> `npm run gate:gateway:preflight`
- Session manager test: `PASS` (`[session_manager] all checks passed`)
- World lock test: `PASS`
- Lint (global): `PASS`
- Build: `PASS`
- Eval planning offline: `PASS` (`fullPassRate=0.8`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `totalAgents=24`, `p95LatencyMs=4434`)
- Gate phase5 hardening: `PASS`
- Gate scale 3000 mock: `PASS` (`runId=fcdc19d4-c48f-44ad-bd9a-c39bd0e5f91d`, `successRate=1`, `fallbackRate=0`, `failureCount=0`, `p95LatencyMs=156373`)
- Gateway preflight: `FAIL` (blocker=`LLM_RELAY_URL` and key chain not configured)
- Snapshot updated at: `2026-03-27 01:02 CST`
