# M09 - Nation and Map Meta Services

## Definition
- Category: Backend
- Owner AI engineer: AI-BE-WorldMeta
- Backup AI engineer: AI-BE-RuleState
- Goal: Nation profiles, founding flow, map overview projections and macro map read models.

## Code Boundaries
- `server/src/application/map/`
- `server/src/application/nation/`
- `server/src/routes/map.ts`
- `server/src/routes/nation.ts`

## Official Entrypoints
- GET /api/map/overview
- GET /api/nation/profiles
- POST /api/nation/found

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-25)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `FAIL` (`162` errors, `3` warnings)
- Lint blocking composition: external legacy cache (removed) = `0`; temporary files `tmp/` = `2` errors; module-owned = `7` errors + `3` warnings
- Build: `PASS` (`npm run build` completed; only bundle-size warnings)
- Eval planning offline: `PASS` (`fullPassRate=0.6`, `tacticalReasonableRate=0.6`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1.0`, `fallbackRate=0`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`deadlock_gate`, `vote_identity_gate`, `anti_tamper_chain_gate` all passed)
- Module lint status: `PASS` (`0` errors, `0` warnings)
- Module lint hit files: none
- Snapshot updated at: `2026-03-25 23:46 CST`

## Validation Update (2026-04-09, W2-C10)
- Scope: 校验并收敛 V2 与主世界链路的同步口径（资源结算 + 同盟快照）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/v2game.ts`
- Behavior updates:
  - 新增 `syncV2StateWithWorld(world)`：为每个主世界势力建立/维护 `v2_auto_<factionId>`，同步占地/城池与资源基线。
  - 新增 `syncWorldFactionResourcesFromV2(world)`：在 `advanceTick` 完成 V2 资源结算后回写主世界势力资源。
  - 新增 `buildV2WorldSyncSnapshot(world)`：输出资源/同盟同步快照（mismatch、缺失玩家、孤儿成员等）。
  - `GET /api/v2/state` 现返回 `sync` 区块，便于前端/HUD直接核验同步一致性。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8793 npm run start` + `GET /api/v2/state` -> `PASS`（`sync.resources.mismatchCount=0`）
  - `npm run sim:quick` -> `EXECUTED`（exit=1，现有 `critical gap`：多阵营扩展语义，非本卡新增）

## Validation Update (2026-04-09, B2-C05)
- Scope: V2 与 world-meta 链路相关基础状态持久化（保证重启不丢同步底座）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `V2GameService` 新增最小持久化：`aiPlayers` / `alliances` / `humanPlayers` / `pityCounters`。
  - 默认落盘路径 `tmp/v2_game_state.json`（支持 `V2_GAME_STATE_PATH` 覆盖）。
  - `syncV2StateWithWorld` 加入变更检测后再触发写盘，减少无意义落盘频率。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8797 npm run start` + `POST /api/v2/alliance` + `GET /api/v2/state` + 重启后 `GET /api/v2/alliances` -> `PASS`

## Validation Update (2026-04-09, B2-C06)
- Scope: V2 状态持久化读取鲁棒性补强（结构清洗 + 损坏文件隔离）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
- Behavior updates:
  - 加载阶段新增最小结构清洗：`AIPlayer/Alliance/HumanPlayer/resources` 按字段裁剪并兜底默认值。
  - 支持 legacy（无 version envelope）对象读取，同时新写入保留 `version/savedAt`。
  - 解析失败时自动把坏文件隔离到 `*.corrupt.<ts>`，避免服务每次启动都被坏 JSON 卡住。
- Validation chain:
  - `npm run build` -> `PASS`
  - legacy payload 加载 smoke -> `PASS`
  - corrupt payload 隔离 smoke -> `PASS`

## Validation Update (2026-04-09, B2-C08)
- Scope: V2 持久化健康指标输出（world-meta 侧可观测）。
- Changed files:
  - `server/src/application/v2/V2GameService.ts`
  - `server/src/app.ts`
- Behavior updates:
  - 新增 `getV2GamePersistHealth()` 并聚合到 `GET /api/health.persistence.v2Game`。
  - 可直接观测 V2 store 的写盘成功/失败次数与隔离恢复次数。
- Validation chain:
  - `npm run build` -> `PASS`
  - `GET /api/health` -> `PASS`（存在 `persistence.v2Game`）

## Validation Update (2026-04-09, B2-C09)
- Scope: 将 v2Game 持久化异常接入 runtime 启动告警分级。
- Changed files:
  - `server/src/app.ts`
- Behavior updates:
  - `persistence.v2Game.persistFailureCount > 0` 时产出 `persist_failures`（`>=5` 升级 HIGH）。
  - `persistence.v2Game.corruptQuarantineCount > 0` 时产出隔离告警（`>=3` 升级 `quarantine_surge/HIGH`）。
  - 告警统一进入 `GET /api/health.persistence.alerts` 与启动日志 `[startup-check][persistence]`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `PORT=8788 npm run start` + `GET /api/health` -> `PASS`（alerts 聚合可读）
