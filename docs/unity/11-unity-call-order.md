# Unity ?????????

?????? Unity ??????????????????????????Unity ????? `/api/unity/*` ?????? `/api/session/*` ????????????????

?????
- `server/src/app.ts`
- `server/src/routes/session.ts`
- `server/src/routes/world.ts`
- `shared/schemas/worldAction.ts`
- `shared/contracts/game/world.ts`
- `shared/contracts/game/session.ts`
- `server/src/multiplayer/SessionManager.ts`

## 1. ?????????

?????????????

```powershell
npm run start
```

???????

```text
http://127.0.0.1:8787
```

## 2. ????

### Step 1 - ??

????????????????

- ?????`GET /api/health`
- ??????`GET /api/unity/runtime`

????

- `GET` ????

???????

- `/api/health`?`ok`
- `/api/unity/runtime`?`tick`?`worldVersion`?`factions[]`
- `factions[]` ???????`factionId`?`autonomyLevel`?`controlMode`?`playerName`?`online`?`hasModelConfig`?`commanderModel`?`generalModel`?`unitModel`

???

- `runtime` ? Unity ????????????????? UI?????????????????????
- ??????????????????? `GET /api/session/status`???? `players[]` ? `aiControlledFactions[]`?

### Step 2 - ??

Unity ??????????? `player` ???

- Endpoint?`POST /api/unity/join`

????

```json
{
  "factionId": "player",
  "playerName": "UnityPlayer"
}
```

???????

- `sessionId`
- `token`
- `factionId`

???

- `token` ???????????????????
- ????? `token` ? 48 ??? hex ????
- ???????????????????????????

### Step 3 - ??

?????????????????????

- Endpoint?`POST /api/unity/heartbeat`

????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef"
}
```

???????

- `ok`
- `error`?????

???

- ???????????? `lastHeartbeat`?
- ????????? `L1_assigned`???????? `L1_assigned`?
- Unity ????????????????????????????

### Step 4 - ????

????????????????

- Endpoint?`POST /api/world/action?includeWorld=1`

????? 1???????

```json
{
  "action": "queuePlanExecution",
  "payload": {
    "plan": {},
    "source": "player",
    "strategicCommand": "Hold the eastern line",
    "requestId": "req-001",
    "basedOnWorldVersion": 123,
    "factionId": "player"
  }
}
```

????? 2????? Tick

```json
{
  "action": "advanceTick"
}
```

???????

- ?????`ok`?`tick`?`worldVersion`?`message`?`world?`
- `queuePlanExecution` ?????/??????`ok`?`message`?`worldVersion`
- `advanceTick` ?????`nationalAgenda`?`courtSession`
- ??????????`unitId`?`domainAgenda`?`domainCommMetrics`?`domainMessages`?`civilMemoryEntries`

???

- `queuePlanExecution` ??? `basedOnWorldVersion`?Unity ??? Step 1 ? Step 5 ???????
- `includeWorld=1` ????????????????????????
- `advanceTick` ???????????????AI ? Unity ????????????

### Step 5 - ????

????????????????????????????

- Endpoint?`GET /api/world?sinceWorldVersion={worldVersion}&intelMode=sparse&planningHistoryLimit=20&replayLimit=20&replayFrameLimit=50`

????

- `GET` ??????? query params

???????

- `world.tick`
- `world.worldVersion`
- `world.map.width`
- `world.map.height`
- `world.map.mapLayoutVersion`
- `world.map.tileStateMode`?`full` / `delta`?
- `world.map.baseWorldVersion`
- `world.map.tileStates`
- `world.factions`
- `world.feedback`
- `world.executions`
- `world.history`

???

- ????????????????? `GET /api/unity/runtime`?
- ?????????????????? `GET /api/session/status`?
- `sinceWorldVersion` ????????????????? full ? delta ???

### Step 6 - ????

?????? AI ????????????

- Endpoint?`POST /api/unity/autonomy`

??????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef",
  "level": "L2_delegated"
}
```

????

- `L1_assigned`
- `L2_delegated`
- `L3_negotiated`

???????

- `ok`
- `error`?????

???

- `GET /api/unity/runtime` ?????? `autonomyLevel` ? `controlMode`?
- ??????????????? `L1_assigned` ???? `L2_delegated`?

### Step 7 - ??

??????????

- Endpoint?`POST /api/unity/leave`

????

```json
{
  "token": "0123456789abcdef0123456789abcdef0123456789abcdef"
}
```

???????

- `ok`

???

- ????`GET /api/unity/runtime` ? `GET /api/session/status` ????????????
- ?? Unity ????????????????? stale ???????

## 3. ??? Unity ?????

1. ????? `GET /api/health`?
2. ?? `GET /api/unity/runtime` ?? `tick`?`worldVersion`??????
3. ???? token???? `POST /api/unity/join`?
4. ?? token ????? `POST /api/unity/heartbeat`?
5. ??????? `POST /api/world/action`?
6. ???? `GET /api/world` ??????????? `GET /api/unity/runtime`?
7. ???? AI ?????? `POST /api/unity/autonomy`?
8. ????? `POST /api/unity/leave`?

## 4. ??????

???????????????????????????

```powershell
npm run start
```

?????????

```powershell
$base = 'http://127.0.0.1:8787'

Invoke-RestMethod -Uri "$base/api/health"
$runtime = Invoke-RestMethod -Uri "$base/api/unity/runtime"

$join = Invoke-RestMethod -Method Post -Uri "$base/api/unity/join" -ContentType 'application/json' -Body (@{
  factionId = 'player'
  playerName = 'Unity11QA'
} | ConvertTo-Json -Compress)

Invoke-RestMethod -Method Post -Uri "$base/api/unity/heartbeat" -ContentType 'application/json' -Body (@{
  token = $join.token
} | ConvertTo-Json -Compress)

Invoke-RestMethod -Method Post -Uri "$base/api/world/action?includeWorld=1" -ContentType 'application/json' -Body (@{
  action = 'advanceTick'
} | ConvertTo-Json -Compress)

Invoke-RestMethod -Uri "$base/api/world?sinceWorldVersion=$($runtime.worldVersion)&intelMode=sparse&planningHistoryLimit=20&replayLimit=20&replayFrameLimit=50"

Invoke-RestMethod -Method Post -Uri "$base/api/unity/autonomy" -ContentType 'application/json' -Body (@{
  token = $join.token
  level = 'L2_delegated'
} | ConvertTo-Json -Compress)

Invoke-RestMethod -Method Post -Uri "$base/api/unity/leave" -ContentType 'application/json' -Body (@{
  token = $join.token
} | ConvertTo-Json -Compress)
```

### 4.1 ????

- `GET /api/health` ?? `ok=true`
- `GET /api/unity/runtime` ?? `tick` / `worldVersion` / `factions[]`
- `POST /api/unity/join` ?? `sessionId`?`token`?`factionId`
- `POST /api/unity/heartbeat` ?? `ok=true`
- `POST /api/world/action` ?? `ok`?`tick`?`worldVersion`?`advanceTick` ???? `nationalAgenda` ? `courtSession`
- `GET /api/world` ?? `world` ????? `map.tileStateMode`?`map.tileStates`
- `POST /api/unity/autonomy` ?? `ok=true`
- `POST /api/unity/leave` ?? `ok=true`

## 5. ??

Unity ???????????????????????????????????????????? `/api/world/action`???????? `/api/world`??????????? `/api/unity/autonomy`?`/api/unity/leave`?
