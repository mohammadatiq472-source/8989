# M18 - Evals Tests and Batch Orchestrator

## Definition
- Category: QA
- Owner AI engineer: AI-QA-Gates
- Backup AI engineer: AI-QA-Gates
- Goal: Official simulation/eval/test pipelines and batch orchestrator quality gates.

## Code Boundaries
- `server/src/evals/`
- `server/evals/`
- `server/tests/`
- `scripts/`
- `server/src/agents/orchestrator/`

## Official Entrypoints
- npm run eval:planning:offline
- npm run eval:orchestrator:stress
- npm run gate:phase5:hardening
- npm run gate:ai:mainline:stability
- npm run gate:ai:nightly:acceptance
- npm run gate:session:security
- npm run gate:harness:isolation
- npm run gate:gateway:preflight
- npm run gate:scale:3000:mock
- npm run gate:scale:3000:gateway
- npm run gate:scale:3000:gateway:ready
- npm run test:session:manager
- npm run sim:13factions

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build` -> `npm run test:world:mutation-lock` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening`
- Lint (global): `PASS`
- Build: `PASS` (`vite build` completed; bundle-size warnings only)
- World lock test: `PASS` (`[world_mutation_lock] all checks passed`)
- Eval planning offline: `PASS` (`fullPassRate=0.8`, `tacticalReasonableRate=0.8`, `acceptabilityRate=1.0`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `failureCount=0`, `totalAgents=24`)
- Gate phase5 hardening: `PASS` (`passed=true`)
- Snapshot updated at: `2026-03-26 22:09 CST`

## Validation Snapshot (2026-03-27)
- Validation chain: `npm run gate:harness:isolation` -> `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening` -> `npm run gate:scale:3000:mock`
- Harness isolation: `PASS`
- Lint (global): `PASS`
- Build: `PASS`
- Eval planning offline: `PASS` (`fullPassRate=0.8`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `totalAgents=24`)
- Gate phase5 hardening: `PASS`
- Gate scale 3000 mock: `PASS` (`successRate=1`, `fallbackRate=0`, `failureCount=0`, `p95LatencyMs=156908`)
- Gateway smoke (24 agents): `FAIL` (`successRate=0`, `fallbackRate=1`, blocker=`missing env LLM_RELAY_URL`)
- Gateway preflight gate: `ADDED` (`npm run gate:gateway:preflight`)
- Snapshot updated at: `2026-03-27 00:36 CST`

## Validation Snapshot (2026-03-27, Session Boundary Hardening)
- Validation chain: `npm run test:session:manager` -> `npm run test:world:mutation-lock` -> `npm run lint` -> `npm run build` -> `npm run eval:planning:offline` -> `npm run eval:orchestrator:stress` -> `npm run gate:phase5:hardening` -> `npm run gate:scale:3000:mock` -> `npm run gate:gateway:preflight`
- Session manager test: `PASS` (`[session_manager] all checks passed`)
- World lock test: `PASS`
- Lint (global): `PASS`
- Build: `PASS`
- Eval planning offline: `PASS` (`fullPassRate=0.8`)
- Eval orchestrator stress: `PASS` (`successRate=1`, `totalAgents=24`, `p95LatencyMs=4434`)
- Gate phase5 hardening: `PASS`
- Gate scale 3000 mock: `PASS` (`runId=fcdc19d4-c48f-44ad-bd9a-c39bd0e5f91d`, `successRate=1`, `fallbackRate=0`, `failureCount=0`, `p95LatencyMs=156373`)
- Gateway preflight: `FAIL` (blocker=`LLM_RELAY_URL` and key chain not configured)
- Snapshot updated at: `2026-03-27 01:02 CST`

## Validation Update (2026-04-09, W2-C08)
- Scope: add AI mainline stability gate covering `tick -> plan queue -> general dispatch -> reflect -> events`.
- Changed files:
  - `server/src/evals/runAiMainlineStabilityGate.ts`
  - `package.json`
- Validation chain:
  - `npm run gate:phase5:hardening` -> `PASS`
  - `npm run sim:13factions` -> `PASS`
  - `npm run gate:ai:mainline:stability` -> `PASS`
- Key assertion coverage (new gate):
  - queue action succeeds and emits `queue_plan_execution` event
  - tick increments after `advanceTickAction`
  - queue event contains `generalDispatch` summary metadata
  - advance event contains reflect metadata (`narrativeEvents`)
  - queue event metadata includes `controlMode` / `autonomyLevel`
- Notes:
  - 13-faction sim completed and output refreshed to `tmp/sim_13f_latest.json`.
  - ReplayRAG cache rename warning (`EPERM` on `tmp/replay_rag_index_cache.json`) observed as non-blocking existing environment noise.

## Validation Update (2026-04-09, B2-C13)
- Scope: 增加 nightly 汇总 gate，统一输出“主链稳定性 + 持久化健康诊断”。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `package.json`
- Behavior updates:
  - 新增正式入口：`npm run gate:ai:nightly:acceptance`
  - gate 内部自动执行 `gate:ai:mainline:stability`，并聚合：
    - mainline gate 结果
    - `factionConfig/aiConfig/v2Game` 持久化健康快照
    - `persistence.alerts` 同口径告警（含允许的 high 告警白名单）
  - 报告落盘路径：
    - `tmp/gates/ai-nightly-acceptance/ai_nightly_acceptance_latest.json`
    - `tmp/gates/ai-nightly-acceptance/<runId>.json`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - 报告文件存在检查 -> `PASS`

## Validation Update (2026-04-10, B2-C15)
- Scope: nightly 汇总 gate 扩展 Session 持久化快照与检查项。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/multiplayer/SessionManager.ts`
  - `server/src/app.ts`
- Behavior updates:
  - `persistence` 快照新增 `session` 分支（来自 `getSessionPersistHealth()`）。
  - `persistence_snapshot_available` 检查新增 `sessionPath`。
  - `persist_failures` 与 `quarantine_detected/surge` 告警聚合新增 `source=session`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run test:session:manager` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（报告中可见 `persistence.session.path`）

## Validation Update (2026-04-10, B2-C18)
- Scope: 把 Session 安全边界 smoke 固化成独立 gate，并并入 nightly 自动验收。
- Changed files:
  - `server/src/evals/runSessionSecurityGate.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `package.json`
- Behavior updates:
  - 新增正式入口：`npm run gate:session:security`。
  - gate 覆盖 join/runtime/autonomy/heartbeat/leave + invalid/expired token 语义。
  - 报告落盘路径：
    - `tmp/gates/session-security-gate/session_security_gate_latest.json`
    - `tmp/gates/session-security-gate/<runId>.json`
  - nightly 汇总新增 `sessionSecurityGate` 节点与检查项：
    - `session_security_gate_exit_zero`
    - `session_security_report_parsed`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run test:session:manager` -> `PASS`
  - `npm run gate:session:security` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`

## Validation Update (2026-04-10, B2-C20)
- Scope: 主线 gate 补“模板动作剧本回放 + events/narratives 对账”自动验收。
- Changed files:
  - `server/src/evals/runAiMainlineStabilityGate.ts`
  - `godot-client/tools/slg_ops_cli.py`
- Behavior updates:
  - `gate:ai:mainline:stability` 现在会执行 `baseline_v1` 模板回放并写入：
    - `tmp/gates/ai_ops_template_replay_latest.json`
    - `tmp/gates/ai-ops-template-replay/<runId>.json`
  - 主 gate 报告新增 `template_replay_passed` 检查与 `templateReplay` 报告索引。
  - CLI 新增 `template-replay` 子命令，支持独立复现同一套回放对账流程。
- Validation chain:
  - `py -3.11 -m py_compile godot-client/tools/slg_ops_cli.py` -> `PASS`
  - `npm run build` -> `PASS`
  - `npm run gate:ai:mainline:stability` -> `PASS`
  - `npm run godot:ops:cli -- --base-url http://127.0.0.1:8801 template-replay --scenario baseline_v1 --report-path tmp/gates/ai_ops_template_replay_cli_latest.json` -> `PASS`

## Validation Update (2026-04-10, B2-C21)
- Scope: 主线 gate 回放链收紧为固定初始态夹具 + 全必选步骤。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiMainlineStabilityGate.ts`
  - `godot-client/tools/slg_ops_cli.py`
- Behavior updates:
  - 新增 `POST /api/save-slots/fixture/prime`，为回放提供可控的 fixture 引导入口。
  - `gate:ai:mainline:stability` 回放阶段固定执行：`backup -> prime(initial_world_v1) -> load fixture -> replay -> restore`。
  - `template-replay` 报告要求 `fixture_slot_primed/fixture_slot_loaded/fixture_backup_restored` 全通过。
  - 回放步骤全必选（不再使用 optional step 放宽策略）。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:mainline:stability` -> `PASS`
  - `py -3.11 godot-client/tools/slg_ops_cli.py --base-url http://127.0.0.1:8799 template-replay --scenario baseline_v1` -> `PASS`

## Validation Update (2026-04-10, B2-C22)
- Scope: nightly 汇总门禁并入 fixture 回放显式断言，避免只看 mainline 退出码。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
- Behavior updates:
  - 新增 `templateReplayGate` 报告节点：
    - `runId/passed`
    - `fixtureChecksPassed`
    - `allStepsRequired`
    - `requiredFailedSteps`
  - 新增 nightly 检查项：
    - `template_replay_report_parsed`
    - `template_replay_passed`
    - `template_replay_fixture_checks_passed`
    - `template_replay_all_steps_required`
  - fixture 必过检查固定为：`fixture_slot_primed`、`fixture_slot_loaded`、`fixture_backup_restored`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`

## Validation Update (2026-04-10, B2-C23)
- Scope: Batch2 门禁最终归档，固化三件套单入口与统一交接 runbook。
- Changed files:
  - `package.json`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
  - `docs/CLOSEOUT_B2_C23_2026_04_10.md`
- Behavior updates:
  - 新增正式入口：`npm run gate:ai:trio`
    - 顺序执行 `gate:session:security -> gate:ai:mainline:stability -> gate:ai:nightly:acceptance`
  - 新增单文档交接入口，统一沉淀：
    - 三件套命令
    - latest 报告路径
    - 模板回放 4 项显式断言
    - 失败排障矩阵
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-10, B3-C01)
- Scope: 三件套提速，nightly 增加“复用最新报告”模式并补新鲜度检查。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `package.json`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - nightly 新增 `executionMode: full | reuse_latest_reports`。
  - 环境变量：
    - `NIGHTLY_REUSE_LATEST_GATES=1`：启用复用模式
    - `NIGHTLY_REUSE_MAX_REPORT_AGE_SEC`：复用报告最大允许年龄（默认 900 秒）
  - 新增检查：
    - `mainline_report_fresh_when_reuse`
    - `template_replay_report_fresh_when_reuse`
    - `session_security_report_fresh_when_reuse`
  - `gate:ai:trio` 已切到复用模式，避免 nightly 内重复执行 session/mainline。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-10, B3-C02)
- Scope: 三件套报告聚合，输出单 JSON 摘要供后续 AI 快速读取。
- Changed files:
  - `server/src/evals/runGateTrioSummary.ts`
  - `package.json`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - 新增 `gate:ai:trio:summary`。
  - `gate:ai:trio` 末尾自动执行 summary，落盘：
    - `tmp/gates/gate-trio/gate_trio_summary_latest.json`
    - `tmp/gates/gate-trio/<runId>.json`
  - 摘要包含：
    - 四份 latest 报告核心状态
    - nightly 对 latest runId 引用一致性校验
    - fixture 与全必选步骤校验
    - `overallPassed` 汇总结论
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-10, B3-C04)
- Scope: nightly 持久化快照扩展 Save Slot 健康字段并纳入可判定项。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/app.ts`
  - `server/src/application/world/WorldService.ts`
- Behavior updates:
  - nightly `persistence_snapshot_available` 现在额外校验 `saveSlotsPath`。
  - nightly 报告 `persistence` 节点新增 `saveSlots` 子结构，便于门禁侧直接判断：
    - `slotCount`
    - `persistFailureCount`
    - `corruptQuarantineCount`
    - `persistVersion`
  - 告警聚合 `alerts.source` 支持 `saveSlots`。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`

## Validation Update (2026-04-10, B3-C05)
- Scope: Save Slot 文件体积门禁化（soft/hard 阈值 + 显式检查项）。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/app.ts`
- Behavior updates:
  - nightly 新增检查项：`save_slots_not_hard_oversize`。
  - nightly `alerts` 新增 Save Slot 体积告警码：
    - `save_slots_oversize_soft`（medium）
    - `save_slots_oversize_hard`（high）
  - 当前默认阈值：
    - soft `128 MiB`
    - hard `512 MiB`
  - 运行样本（2026-04-10）：
    - `fileSizeBytes=165402869`，`fileSizeLevel=soft`
    - gate 通过（非 hard），并输出中告警提示后续归档治理。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`

## Validation Update (2026-04-10, B3-C06)
- Scope: nightly 纳管 Save Slot 归档/锁治理状态，补齐多实例一致性最小门禁。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/app.ts`
  - `server/src/application/world/WorldService.ts`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - nightly `alerts` 新增 Save Slot 治理告警码：
    - `save_slots_archive_failures`
    - `save_slots_lock_contention`
    - `save_slots_lock_failures`
  - nightly `checks` 新增：
    - `save_slots_archive_failures_absent`
    - `save_slots_lock_contention_absent`
    - `save_slots_lock_failures_absent`
  - runtime `/api/health.persistence.alerts` 同步支持以上三类告警，便于值班和门禁共享语义。
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`（新增三项检查均为 `true`）

## Validation Update (2026-04-11, B3-C07)
- Scope: nightly 纳管 Save Slot 归档恢复演练结果，补“可恢复性”显式检查。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
  - `server/src/routes/observability.ts`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - nightly 新增检查项：
    - `save_slots_archive_restore_drill_passed`
  - nightly 执行 restore drill（dry-run）并写入检查详情：
    - `status/archivePath/archiveCount/slotCount/persistVersion/message`
  - 告警新增：
    - `save_slots_restore_drill_failures`（restore drill 失败计数）
  - API 新增正式入口：
    - `GET /api/save-slots/archive`
    - `POST /api/save-slots/archive/restore-drill`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-11, B3-C08)
- Scope: Save Slot 真实恢复入口上线并纳入 persistence alert 语义。
- Changed files:
  - `server/src/application/world/WorldService.ts`
  - `server/src/routes/observability.ts`
  - `server/src/app.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
- Behavior updates:
  - 新增 API：`POST /api/save-slots/archive/restore`（受控恢复入口）。
  - 新增 runtime/nightly 告警码：
    - `save_slots_restore_apply_failures`
  - `persistence.saveSlots` 指标新增 restore apply 维度：
    - `restoreApplySuccessCount/restoreApplyFailureCount`
    - `lastRestoreApplyStatus/lastRestoreApplyMessage`
    - `lastRestoreApplyArchivePath/lastRestoreApplyBackupPath`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npx tsx tmp/verify_b3c08_restore_apply.ts`（临时验证）-> `PASS`

## Validation Update (2026-04-11, B3-C09)
- Scope: nightly 纳管 restore apply 可观测门禁，并新增 rollback drill gate。
- Changed files:
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `server/src/application/world/WorldService.ts`
  - `server/src/app.ts`
  - `server/src/routes/observability.ts`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - nightly 新增检查项：
    - `save_slots_restore_apply_metrics_observable`
    - `save_slots_restore_apply_failure_rollback_trace`
    - `save_slots_archive_restore_rollback_drill_passed`
  - nightly 增加回滚演练执行（隔离 drill store）：
    - 产出 `status/archivePath/backupPath/drillStorePath/rollbackVerified`
    - 不改变 active save-slot store
  - runtime/nightly 告警新增：
    - `save_slots_restore_rollback_drill_failures`
  - API 新增正式入口：
    - `POST /api/save-slots/archive/restore-rollback-drill`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-11, B3-C10)
- Scope: Save Slot restore apply 三段行为正式 gate 化，并接入 nightly 主门禁。
- Changed files:
  - `server/src/evals/runSaveSlotsRestoreApplyGate.ts`
  - `server/src/evals/runAiNightlyAcceptanceGate.ts`
  - `package.json`
  - `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
- Behavior updates:
  - 新增正式 gate：
    - `gate:save-slots:restore-apply:stability`
    - 报告路径：`tmp/gates/save-slots-restore-apply-gate/save_slots_restore_apply_gate_latest.json`
  - nightly 新增检查项：
    - `save_slots_restore_apply_gate_exit_zero`
    - `save_slots_restore_apply_gate_report_parsed`
    - `save_slots_restore_apply_gate_passed`
  - 临时验证脚本已下线：
    - `tmp/verify_b3c08_restore_apply.ts` 删除
- Validation chain:
  - `npm run gate:save-slots:restore-apply:stability` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`

## Validation Update (2026-04-11, B3-C11)
- Scope: Save Slot restore apply gate 产物保留收敛，并把 restore-apply 关键结果并入 trio summary。
- Changed files:
  - `server/src/evals/runSaveSlotsRestoreApplyGate.ts`
  - `server/src/evals/runGateTrioSummary.ts`
  - `docs/modules_v2/M01.md`
  - `docs/modules_v2/M18.md`
- Behavior updates:
  - `runSaveSlotsRestoreApplyGate` 新增产物保留策略（latest + 最近 N 份）：
    - 保留 `save_slots_restore_apply_gate_latest.json`
    - 按最近时间保留 `N` 份 stamped 报告与对应 `<runId>_workspace`
    - 超额历史产物自动清理并写入 `report.artifacts`
  - 新增保留策略参数：
    - `SAVE_SLOTS_RESTORE_APPLY_GATE_KEEP_RECENT`（默认 `12`）
  - `runGateTrioSummary` 新增 restore-apply gate 汇总节点：
    - `reports.saveSlotsRestoreApply`
    - 字段包含 `failedChecks`、三段状态、健康计数、保留统计
  - trio summary 新增 restore-apply 三项一致性检查：
    - `save_slots_restore_apply_gate_passed`
    - `nightly_references_latest_restore_apply_gate`
    - `save_slots_restore_apply_three_phase_semantics`
- Validation chain:
  - `npm run build` -> `PASS`
  - `npm run gate:ai:nightly:acceptance` -> `PASS`
  - `npm run gate:ai:trio` -> `PASS`
