# M16 - Frontend Map Rendering

## Definition
- Category: Frontend
- Owner AI engineer: AI-FE-MapSurface
- Backup AI engineer: AI-FE-CommandSurface
- Goal: Pixi map rendering, viewport logic, LOD textures and map interaction fidelity.

## Code Boundaries
- `src/components/pixi/`
- `src/components/MapBoard.tsx`
- `src/components/mapViewport.ts`

## Official Entrypoints
- npm run dev

## Handoff Checklist
- [ ] Reproduce module baseline from official entrypoints
- [ ] Validate cross-module impacts before merging
- [ ] Update this card when interfaces or ownership change

## Validation Snapshot (2026-03-26)
- Validation chain: `npm run lint` -> `npm run build`
- Lint: `PASS`
- Build: `PASS` (`vite build` completed; bundle-size warnings only)
- Module lint status: `PASS`
- Snapshot updated at: `2026-03-26 21:19 CST`

## Validation Update (2026-04-09, W2-C12)
- Scope: 在不改语义前提下继续优化 Godot MapGrid 大图渲染。
- Changed files:
  - `godot-client/scripts/map/map_grid.gd`
  - `godot-client/README.md`
- Key outcomes:
  - 在可见区域裁剪基础上增加自适应采样步长（`sampleStep`），按可见候选量动态降采样。
  - HUD 性能指标新增 `visibleCandidates` 与 `sampleStep`，便于直接识别“候选面”与“实际绘制量”的差距。
  - 基线导出 JSON 新增 `metrics.visibleCandidates` 与 `metrics.sampleStep` 字段，支持前后版本量化对比。
- Validation chain:
  - `D:\\Apps\\Godot\\Godot_v4.6.2-stable_win64_console.exe --headless --path godot-client --quit-after 1` -> `PASS`
  - `npm run gate:godot:week1` -> `PASS`
