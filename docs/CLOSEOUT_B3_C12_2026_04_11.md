# 收尾总览（B3-C12，2026-04-11）

## 0. 结论

`B3-C12` 已完成：`gate-trio` summary 产物已收敛为 `latest + 最近 N 份` 保留策略，并新增 summary 新鲜度显式检查。

## 1. 改动文件

1. `server/src/evals/runGateTrioSummary.ts`
2. `docs/modules_v2/M18.md`
3. `docs/TASK_2026_04_10_AI_BATCH3_EXEC_CARDS.md`
4. `docs/GATE_TRIO_HANDOFF_2026_04_10.md`
5. `docs/AI_QUICK_NAV_INDEX_2026_04_10.md`
6. `docs/AI_ENGINEER_HUB_2026_03_25.md`
7. `docs/CLOSEOUT_B3_C12_2026_04_11.md`

## 2. 行为变更

1. Gate Trio summary 产物保留策略：
   - 固定保留 `tmp/gates/gate-trio/gate_trio_summary_latest.json`
   - 仅保留最近 `N` 份 stamped summary 报告
   - 超额历史报告自动清理
2. 新增保留参数：
   - `GATE_TRIO_SUMMARY_KEEP_RECENT`（默认 `12`，范围 `1~200`）
3. 新增 freshness 参数：
   - `GATE_TRIO_SUMMARY_MAX_REPORT_AGE_SEC`（默认 `900`）
4. summary 新增 5 项 freshness 检查：
   - `session_security_report_fresh`
   - `mainline_report_fresh`
   - `nightly_report_fresh`
   - `template_replay_report_fresh`
   - `save_slots_restore_apply_report_fresh`
5. summary 新增 `policy` 与 `artifacts` 元数据：
   - `policy.maxReportAgeSec/keepRecent`
   - `artifacts.retainedRunIds/prunedRunIds/prunedReports`

## 3. 验证链

1. `npm run build` -> `PASS`
2. `npm run gate:ai:nightly:acceptance` -> `PASS`（runId: `ai_nightly_2026-04-11T08-00-56-830Z`）
3. `npm run gate:ai:trio` -> `PASS`（nightly runId: `ai_nightly_2026-04-11T08-06-56-791Z`，summary runId: `gate_trio_summary_2026-04-11T08-07-14-754Z`）
4. 抽样检查：
   - `policy.maxReportAgeSec=900`
   - `policy.keepRecent=12`
   - `artifacts.prunedReports=1`
   - 5 项 freshness checks 全部为 `true`

## 4. 风险与边界

1. 当前 freshness 默认阈值 `900` 秒，若本地只跑 `gate:ai:trio:summary` 且上游报告过旧，summary 将按预期失败。
2. 本卡仅收敛 `gate-trio` summary 产物；其他 gate 目录仍沿各自既有保留策略。

## 5. 下一步建议

1. 推进 `B3-C13`：补 `gate:ai:trio:summary` 单独执行场景的陈旧报告分级提示与排障指引。
